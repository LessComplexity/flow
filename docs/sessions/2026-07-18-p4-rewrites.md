# Session 12 — 2026-07-17/18 — P4 flow-rewrite + two interp P0s + P5 prep

Orchestrator: Claude (Fable 5) · design review: 17-agent workflow (4 Opus lenses + per-finding verifiers) · implementation: 10-agent workflow (5 sequenced Opus TDD WPs + 4 adversarial reviewers + fixer) · orchestrator line-by-line review + 2 root-cause fixes after. Immutable log (ADR-0017). Session scope set by Sapir: originally "complete all through M5"; revised mid-session to **close after P4** — P5–M5 carry to S13 with prep banked.

## 0. Continuation brief

Current state: **P4 complete, workspace 511 green** (192+102+128+29+37+23), fmt+clippy clean, all docs reconciled, committed (see §8). No live jobs/machines (vastai: no instances; the two S12 workflows completed and are dead).
Next step: P5 backend-llvm per the already-written `components/backend-llvm/DESIGN.md` + ADR-0020 — start with an adversarial design-review pass, then the implementation workflow.
Resume command/check: read `docs/next-session.md`, then `cargo test --workspace` (expect 511).

## 1. Work completed

- **rewrite DESIGN.md** (model-first, ADR-0014): plan+replay architecture (analysis `→ RewritePlan`, one shared builder-backed replayer), R1 oracle-equality contract (`Done` byte-exact; `Trapped ≈ Trapped` ⊥-identified; `Diverged ≈ Diverged`; fuel-insensitive), R2–R6, §1.2 plan laws P1–P3, RW1–RW11 ledger. Plus `plans/plan-rewrite.md` (6 WPs).
- **Adversarial design review pre-code** (4 lenses × verify): 13 findings → 8 CONFIRMED / 5 refuted. Blockers folded in before implementation: Return-writer dropping (3 lenses converged → P1), loop-state constify → `LoopBackOutsideScc` (live builder repro → P2), fusion `Diverged↔Trapped` flip (→ P3), dead pure loops lower-reachable (→ RW11), nested-loop reachability correction (→ RW8 whole-graph identity), example count 11→10 (`vector.flow` out-of-Core).
- **`crates/flow-rewrite` implemented** (~3.9k lines src+tests): `plan.rs`, `replay.rs` (recipe classification incl. loop quartet, fused-body inline synthesis, fn-level DCE), `equations.rs` (oracle-exact folds + identities + forwards), `graph_rewrites.rs` (conservative DCE + CSE value numbering), `functor_laws.rs` (Map∘Map fusion + map(id)), `naturality.rs` (layer-2 law table, data only), `driver.rs` (by-value fixpoint, MAX_ROUNDS 32, §9.6 report). 23 tests: identity anchor (10 examples byte-equal), R1 property battery (per-pass + full × closed/open × default/trap_free + determinism + idempotence), 4 adversarial pins, per-rule micros, goldens (interp-exact + Mermaid + report snapshots), `rewrite_scale` bench.
- **testgen** (`tests/testgen/mod.rs`): random Core-program generator over the public builder (closed effectful/pure + open fns; trap_free mode) — the P5–P7 differential input source (HANDOFF §9).
- **Two interp P0s fixed** (both found because Sapir asked for a matmul example):
  1. **Multi-hop computed loop-invariants** (`x*2`, `a[i*4+k]`) panicked the driver read-before-write — root cause in `flow-ir::topo_order` (FIFO Kahn released `LoopEnter` before derived invariants). Fix: **LoopEnter deferral** (released only when nothing else is ready) — "invariants precede the header" is now a theorem every consumer (incl. P5+ backends) inherits. Pins: ir `topo_orders_multi_hop_invariants_before_loop_enter`, interp `loop_invariants.rs` (incl. **matmul4** — a full 4×4 matmul with a real k-loop, now a permanent regression).
  2. **Two sequential loops in one fn** (legal Core) tripped `derive_plan`'s M1 assert — per-fn SCC-**union** attribution. Fix: per-merge-SCC attribution; same flaw fixed in rewrite's `is_canonical`/`exit_of` (route-feeder rule) so two-loop fns are **rewritable**, not identity-skipped. Pins: interp `two_sequential_loops_in_one_fn`, rewrite `two_sequential_loops_rewrite_not_skipped`.
- **P5 prep banked:** **ADR-0020** (backend emission contract: `emit(&CategoryIr) -> Result<String, EmitError>` by convention; shared **`flow-rt`** runtime for print/trap oracle-parity; wrapping ints no-`nsw`; exit-101 traps; differential duties; deterministic emission) + **backend-llvm DESIGN.md** (alloca-slot piecewise emitter, strict-Phi-as-`select`, ADR-0016 loop CFG, harness + sepia perf baseline spec). Toolchains: verilator + icarus-verilog installed (P7); vastai CLI verified, zero instances (P6); clang present.
- **Design discussion (Sapir):** array-element write → `docs/notes/array-update-design.md` (recommendation: pure `Update` op + `c[i] <- x` mut-rebind desugar, in-place lowering via the E3 last-use frontier — the IoToken-linearity play for arrays; `tabulate` as the closure-era successor that deduces `update` away). ADR candidate when Core+1 opens.

## 2. Decisions

| Decision | Verdict | Why |
| --- | --- | --- |
| Rewrites rebuild through `IrBuilder` (plan+replay) vs a flow-ir mutation API | kept rebuild (RW1) | ir §17 sanctions it; well-formed by construction; bench shows ~linear, nowhere hot |
| R1 `≈`: traps ⊥-identified, fuel-insensitive | kept (RW2) — **flagged to Sapir** | rebuild permutes trap identity among independent morphisms; §2.6 error monad has one ⊥ |
| Plan laws P1 (Temporary-only keys), P2 (SCC-respecting), P3 (loop-free fusion bodies) | adopted into DESIGN §1.2 | three CONFIRMED review blockers converged; P1 lossless for Return (a sink) |
| Non-canonical loops ⇒ whole-graph identity (by-value return) | kept (RW8) | multi-merge shape IS lower-reachable but hits interp's own M1 assert; generic-SCC replay = recorded increment |
| Exit attribution: route-feeder-in-merge's-SCC, never reachability/union | adopted (interp + rewrite, S12) | the union/reachability pattern broke two-sequential-loops in both crates |
| CSE constant dedup | omitted as-built | contradicts P1; headroom via a replay-side channel (suggestions #7) |
| DCE precision (`Div/Mod/Index/Call/Map/Fold` removability refinements) | deferred, conservative keep | superset-conservative is R1-safe; 16k+ property programs green |
| ADR-0020 contract-by-convention + `flow-rt` (no shared trait/crate for the contract) | accepted, flagged | YAGNI until the CLI (third consumer); flow-rt kills float-format + trap-UB drift classes for all three backends |
| Session scope | revised by Sapir: close after P4 | P5–M5 → S13; prep banked so S13 starts at implementation |

## 3. Tests, checks, benchmarks

| Check | Result | What it proved |
| --- | --- | --- |
| `cargo test --workspace` | 511 passed, 0 failed | P4 DoD + both P0 fixes, no regressions (was 484 at S11) |
| R1 property battery (48 cases/suite; 16k+ programs at PROPTEST_CASES=2000 during review) | green | every pass + full rewrite interpreter-equal, validate-clean, deterministic, idempotent |
| `matmul4` via interp | `8\n136\n` | the user-found P0 class fixed end-to-end |
| two-loops via interp / rewrite | `20\n` / not-skipped + equal | per-merge attribution in both crates |
| `rewrite_scale` bench | chain 1k/10k/100k: 720µs/7.75ms/87ms; grid CSE: 257µs/2.3ms/23ms | rebuild-not-mutate holds; ~linear |
| fmt + clippy (workspace, all targets) | clean / 0 warnings | gate |

## 4. Live handoff state

| Type | Handle / location | State | Inspect / resume | Stop / cleanup |
| --- | --- | --- | --- | --- |
| branch | `main` | clean after commit (§8) | `git status` | none |
| machine/job | vast.ai | **no instances** (verified S12) | `vastai show instances` | n/a — rent at P6 |
| toolchain | verilator + icarus (brew, S12), clang (homebrew llvm) | installed | `which verilator iverilog clang` | keep (P7/P5) |
| artifact | scratchpad matmul/two-loops `.flow` repros | superseded — all promoted to in-repo tests | `cargo test -p flow-interp --test loop_invariants` | scratchpad is session-temp; nothing to keep |

## 5. Open items

| Priority | Item | Doc/code reference | Next action | Done when |
| --- | --- | --- | --- | --- |
| P0 (S13) | P5 backend-llvm (M2) | `components/backend-llvm/DESIGN.md` + ADR-0020 | adversarial design review → implement flow-rt + emitter + differential harness | 10 examples + testgen differential green; sepia baseline in STATUS |
| P1 | Sapir ratifications | next-session.md "Open questions" | Sapir reads: ADR-0020, RW2, array-update note (+ carried ADR-0016/0013/IN6/OQ1–8) | verdicts recorded |
| P2 | rewrite headroom | `components/rewrite/suggestions.md` | any session; all R1-safe | rows applied or re-parked |
| P3 | P6 CUDA (vast.ai) → P7 Verilog → M5 CLI | tasks #4–#6 / STATUS rows | after P5 | per HANDOFF §8 DoDs |

## 6. Architecture / model changes

New Level-B component modeled + built: `rewrite` (INDEX row modeled; plan+replay `Dat`/`Trn`, R1–R6 composition rules). `backend-llvm` modeled (INDEX row modeled; emitter `Dat`/`Trn`, L1–L4). ir §13 topo gains the LoopEnter-deferral clause (a strengthened deduced-morphism contract); interp §4 attribution clarified per-merge (scope: multiple sequential loops per fn now in M1). ADR-0020 fixes the strategy-2-category backend contract (categorical-model §7.5 candidate #1 → done). No Level-A/spec change; no coherence-law status change (map §4.5 all-PASS stands — rewrite is same-`Loc` `Dat`+`Alg`).

## 7. Docs reconciled

| Doc | Change |
| --- | --- |
| `components/rewrite/{DESIGN,IMPLEMENTATION,STATUS,suggestions,plans/plan-rewrite}.md` | DESIGN written + review findings + as-built deltas; functor map; status tested; headroom; plan As-built |
| `components/backend-llvm/DESIGN.md` | written (P5-ready) |
| `components/{ir,interp}/DESIGN.md` + `STATUS.md` | LoopEnter deferral (ir §13); per-merge attribution + scope (interp §4); counts 102/37; S12 fix notes |
| `docs/{STATUS,IMPLEMENTATION,suggestions,architecture-map,next-session}.md` + `architecture/INDEX.md` | phase P4-complete; rewrite rows built/tested; ADR-0020 ledger row; S12 session row; suggestion #1 done + #7 added; INDEX rewrite+backend-llvm modeled |
| `docs/decisions/ADR-0020-backend-emission-contract.md` | new (flagged for Sapir) |
| `docs/notes/array-update-design.md` | new (Sapir design question, S12) |

## 8. Files changed

`crates/flow-rewrite/**` (new crate: 8 src + 6 test files + bench + snapshots) · `crates/flow-ir/src/algo.rs` + `tests/algos.rs` (LoopEnter deferral + pin) · `crates/flow-interp/src/loops.rs` + `tests/loop_invariants.rs` (per-merge attribution + 3 pins) · docs per §7 · committed this session (see `git log`).
