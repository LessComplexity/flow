# 2026-07-22 — S21: ADR-0029 stage 2 + widen + WP3b + the full perf matrix

Orchestrator: Claude Fable (category-architect skill). Immutable log (ADR-0017).
**Session structure per Sapir's new split (recorded in memory): codex CLI (gpt-5.6-sol, xhigh) codes/debugs; the orchestrator designs, dispatches, reviews every diff line-by-line, fixes substandard work, runs the box, and reconciles docs.** Five codex WPs dispatched; four landed and were review-fixed; one stalled and was implemented by the orchestrator inline.

## 0. Continuation brief

Current state: **S21 closed clean — all three S21 P0s discharged; workspace 841 green (200 syntax · 144 ir · 154 lower · 29 check · 62 interp · 63 rewrite · 27 llvm + 1 ignored perf · 1 flow-rt · 161 cuda; flow-cli = 2 bins, no tests); fmt clean; both S21 boxes destroyed; tree uncommitted (S14–S21, Sapir owns commits).** ADR-0029 stage 2 fully shipped (cuda iota/fill kernels; `Operation::Widen` + `widen_i64/f32/f64` full-pipeline; procedural v2 bench sources), llvm WP3b killed the aggregate-move optimizer blowup, one box run delivered the remote cuda differential (15/15 — after a rewrite-fixpoint P0 it exposed was fixed in-session), the FLOW_PERF re-measure, and the full sweep N=4→512 all legs (first-ever flow-llvm N=128/256/512 rows). `docs/performance/matmul.md` rewritten.
Next step: **S22 per `docs/next-session.md`** — Sapir commits the tree, then the accepted `crates/backends/{b}` folder move (ADR-0030 §Folder move), then the P1 waves (arena v1.1, region step 2, tree-fold).
Resume command/check: read `docs/next-session.md`; `cargo test --workspace`.

## 1. Work completed

**Plans/design (model-first, §6.1):** `components/backend-cuda/plans/plan-iota-fill-stage2.md` (written → SHIPPED same day, as-built deviations recorded); ADR-0029 **Amendment** (widen realization: `Operation::Widen` 33rd variant, four-edge lattice `i32→i64 · i32→f32 · i32→f64 · f32→f64` — `i64→f64` excluded as not value-exact; surface = the `widen_i64/f32/f64` builtin family because lower's forward-synthesis typing makes a bare `widen` ambiguous); **ADR-0030 candidate** (external-backend protocol: serialized versioned `CategoryIr` bundle + deduced queries over a subprocess contract; SDK = schema + reader + the oracle conformance kit; folder move `crates/backends/{b}` ACCEPTED, names unchanged — Sapir's S21 directive).

**WP1 (codex, review-fixed) — cuda `Iota`/`Fill` kernels (the 5th `Unsupported` cell):** bulk-site family + `iota_kernel`/`fill_kernel`, arena members, NO trap plumbing, count as a `long long n` launch arg — deliberately stronger than the plan: equal-shape kernels dedup ACROSS counts (`iota(4)`/`iota(5)` → one kernel). Twins = local for-loops. Orchestrator review fixes: elem ctype derived (not `(int32_t)` hardcoded — the ADR's `[i64;n]` form must not silently truncate) ×2 paths; the missing rewritten differential leg added (— the leg that later caught the fixpoint P0 on hardware).

**WP2 (codex, review-fixed) — the widen wave across 7 crates:** ir op + builder + validate arm (`InvalidWiden` twins) + §5.1 typing rows; interp `as`-semantics arm (boundary contract: i32::MIN, 2^24+1, −2000000001, 0.1f32); lower builtin family (`is_collection_builtin` → `is_pure_builtin` + `widen_target`, one predicate across 4 routing sites; L1614 teaching diagnostic; L1009); rewrite replay arm + oracle-exact const fold + testgen `Step::Widen` (all four edges, i64/f32/f64 pools, float prints); llvm `sext`/`sitofp`/`fpext`; cuda C-cast arms (scalar — no kernel, no arena; pins prove it). Orchestrator review fix (**the Dest-contract refactor**): builder `widen` hardcoded `Dest::Fresh(None)` breaking the family API — bind names never reached IR objects and lower/replay both post-wired a gratuitous `Output`; threaded `Dest` through builder + lower + replay + 16 call sites, snapshot regenerated with names (`a: i64`…).

**WP3 (codex) — procedural v2 generators:** same formula/seeds/filenames; `iota(NN) -> map { t -> … -> widen_f64 }` replaces literal tables AND the v1 enumerate+seed-matrix trick; sources 3.8 MB → 72 KB (98%); .cu 1.3 MB → 12 KB; N=512 set added; oracle gates reproduced EXACTLY (−275/3748 @4, 1815/6944 @16 — both forms). Its honest failure report exposed that BL1 had a third face…

**WP3b (codex, no fixes needed) — llvm first-class aggregate-move elimination:** the v2 21 KB module STILL OOM-killed clang -O2 — diagnosis (orchestrator): staging built capture products via `load [65536 x double]` whole-array SSA values (multi-hundred-KB aggregates blow up LLVM's own optimizer; v1's literal wall had masked it). Fix: an array value never moves as a first-class SSA aggregate — pointer-only staging fields (`pointer_only_array_component`/`lower_slot_ty`, per-op address-use rules) + `llvm.memcpy` for owed copies (`dst==src` identity skip preserves the S20 in-place elision) + array Phi = `select` over ptrs. **matmul256_cap clang -O2: OOM-kill/222 s+ → 0.08 s / 57 MB (independently reproduced); matmul512 same.** 3 snapshots re-pinned (hand-read), `capture_array_staging_never_loads_whole` regression, 1,280-run differential green at both opt levels post-change.

**WP4 (orchestrator) — the combined box run** (2 instances: 45516002 flaky-`loading` 15 min → destroyed; 45516809 did all work → destroyed): remote cuda differential; FLOW_PERF re-measure; full sweep N=4→512 (runner extended to 512 + the llvm big-N legs). Sweep-1 exposed missing llvm/numpy legs → **box clang was 14** (predates opaque-`ptr`; silent skip) → clang-15 + numpy installed, `s21_box.sh` now guards; sweep-2 completed the matrix (86 rows). Survived a local machine crash mid-run (box-side nohup work recovered, not re-run).

**WP5 (orchestrator inline; codex stalled 1 h with zero edits, killed) — the rewrite-fixpoint P0** the box differential's rewritten iota/fill leg exposed: `MAX_ROUNDS hit without a fixpoint` (driver.rs:120). Diagnosis (traced to the object): CSE aliases the two structurally-equal `(7,4)` fill-tuples; replay's `Fill` arm called the minting `fill(x, n)` sugar, resurrecting the eliminated duplicate every round (`Iota`'s arm was already faithful — it passes the count object). Fix = **replay faithfulness**: `IrBuilder::fill_from(pair, dest, loc)` (emits the `Fill` edge from the existing tuple, mints nothing; slot-1 feeder scanned via `in_edges`; same static-n rule) + the replay arm swap + testgen `Step::Iota`/`Step::Fill` draws (the coverage gap that let it ship in S20) + directed regression `rewrite/tests/fixpoint.rs`. Re-verified on the box: **differential 15/15 incl. `differential_iota_fill` raw+rewritten.**

**Perf doc:** `docs/performance/matmul.md` rewritten (S21 main table, ratios, throughput anchors, FLOW_PERF decomposition, verification, method; S20 table archived in-doc — its raw CSV was overwritten pre-archive, recorded honestly).

**Also:** memory updated (codex delegation split); `docs/STATUS.md` ledger gained the missing ADR-0028/0029 rows + ADR-0030; capability matrix (iota/fill cuda ✅, widen row); all component docs reconciled in-session (not post-hoc).

## 2. Decisions

| Decision | Verdict | Why |
| --- | --- | --- |
| Delegation split | Sapir: codex (gpt-5.6-sol xhigh) codes/debugs; Fable designs/orchestrates/reviews/reconciles | recorded in memory; 4/5 WPs landed via codex |
| S21 scope | stage 2 first, ONE combined box run for all three P0s | Sapir picked the recommended option; saved a box cycle |
| widen surface | `widen_i64/f32/f64` family, NOT bare `widen` | forward-synthesis typing can't infer the target; i32 has 3 successors; matches width-explicit L-code discipline; zero parser change (ADR-0018 name path) |
| widen lattice | 4 edges; `i64→f64` excluded | not value-exact above 2^53 — "safe-widening" premise breaks |
| iota/fill kernel count | launch ARG, not baked literal | cross-count #17 dedup (kept from codex's deviation — an improvement) |
| WP3b fix shape | pointer-only staging + memcpy, never aggregate SSA | root cause (optimizer scaling), not symptom; per-op address-use analysis conservative-correct |
| Fixpoint fix shape | replay faithfulness (`fill_from`), NOT CSE-side exclusions | any future pass would trip on re-minting; recorded as a load-bearing invariant |
| Stalled codex (WP5) | killed at 1 h/zero edits; orchestrator implemented inline | box billing; fix small + fully designed; split's "fix substandard work" clause |
| Folder move `crates/backends/{b}` | ACCEPTED (Sapir directive); execute post-commit as its own atomic commit; names unchanged | zero source edits; structure states the strategy-2-category fact; ADR-0030 §Folder move |
| External-backend protocol | ADR-0030 candidate: serialized CategoryIr + subprocess + oracle conformance kit; dlopen/C-ABI rejected | Rust has no stable ABI; conformance = the same differential our backends pass |
| S20 raw CSV loss | overwritten by the runner pre-archive; numbers preserved in matmul.md archive + S20 log | recorded honestly; "back up results.csv before any sweep" gotcha added |

## 3. Tests, checks, benchmarks

| Check | Result | What it proved |
| --- | --- | --- |
| `cargo test --workspace` at close | **841 green**, fmt clean | the whole S21 tree |
| Remote cuda differential (4090, box 45516809) | **15/15** (first run 14/15 — the fixpoint P0; fixed, re-run green incl. `differential_iota_fill` raw+rewritten) | S21 emitter + the fixpoint fix on hardware |
| llvm differential post-WP2 AND post-WP3b | 11/11 each (~330 s; 320 testgen × raw/rewritten × -O0/-O2 = 1,280 compile-runs, zero divergences) | widen + aggregate-elimination semantics |
| clang -O2 gate | matmul256_cap.ll **0.08 s / 57 MB** (was OOM-kill); matmul512 0.09 s / 57 MB; independently reproduced | BL1's optimizer face dead |
| v2 oracle gates | −275/3748 @N=4, 1815/6944 @N=16, both forms, interp + native spot-check | procedural sources value-identical to v1 |
| Cross-implementation verification | flow-cuda ≡ naive-CUDA ≡ cuBLAS ≡ rust ≡ cpp ≡ chapel ≡ numpy outputs at N=64/128/256/512 | five-way independent agreement |
| Sweep (86 rows, `results.csv`) | flow-cuda kernel f32 **812 GFLOP/s @512** (4.0× from naive CUDA); **flow-llvm cap f64 BEATS single-thread C++ @256 (29.2 vs 37.2 ms) and @512 (701 vs 909 ms)**; llvm cap @64 29.17 → **1.77 ms** (16.5×, WP3b) | the S21 perf story; `docs/performance/matmul.md` |

## 4. Live handoff state

| Type | Handle / location | State | Inspect / resume |
| --- | --- | --- | --- |
| branch | `main` | **uncommitted** (S14–S21; Sapir owns commits; the S22 folder move waits on the commit) | `git status` |
| vast.ai | `45516002`, `45516809` (S21's) | **both destroyed** (results local) | — |
| vast.ai | `45510479` (Sapir's pytorch box) | still running at close — **hands-off** | `vastai show instances` |
| artifacts | `benches/matmul/results.csv` (86 rows, S21) · `docs/performance/matmul.md` | landed | — |
| background tasks | none | all codex WPs + box tasks accounted for (WP5 codex killed deliberately) | — |

## 5. Open items (the S22 agenda — full detail in `docs/next-session.md`)

| Priority | Item | Next action | Done when |
| --- | --- | --- | --- |
| P0 | Folder move (ADR-0030 §, accepted) | after Sapir commits: `git mv` + workspace/path-dep edits, names unchanged, one commit | workspace green post-move |
| P1 | Arena v1.1 (18a) | interference coloring + loop-cone two-slot rotation | malloc counts drop in the §7 gates |
| P1 | Region_plan step 2 | solve the multi-merge-SCC oracle boundary | matmul region acceptance |
| P1 | Tree-fold (ADR-0028) | exact-op folds tree-reduced | canonical-tree re-pin green |
| P2 | kernel 4× residue · 17b dedup key · heap lowering · `time` builtin (Sapir) · procedural sepia · chapel-gpu · P7 | as numbers/Sapir direct | — |
| P3 | ADR-0030 protocol | behind the CLI crate | — |

## 6. Architecture / model changes

- **Core `Operation` set → 33 variants** (`Widen`); the realized-op ledger + capability matrix updated; widen surface = 3 reserved builtin names.
- **Replay-faithfulness invariant (new, load-bearing):** replay emits structure from existing remapped objects only — sugar builders that mint are frontend-only; every minting op needs a `*_from` replay entry + a testgen draw in the same change (the S21 fixpoint class).
- **llvm data-movement discipline:** an array value never moves as a first-class SSA aggregate (pointer-only staging / memcpy) — BL1 reduced to its last face (the 8 MB alloca stack ceiling; heap lowering is the remaining headroom).
- **cuda:** iota/fill placed (arena members, trap-free, cross-count kernel dedup); `Unsupported` cells 5 → 4.
- **ADR-0030 candidate:** the backend boundary as a versioned `Trm` (serialized CategoryIr + deduced queries) with subprocess realisations; in-tree backends group under `crates/backends/` (accepted).

## 7. Docs reconciled

| Doc | Change |
| --- | --- |
| `docs/STATUS.md` | S21 header/phase; 6 component rows + Tests cells; capability matrix (iota/fill ✅, widen row); ledger +ADR-0028/0029 rows (S20 gap) + ADR-0030 |
| `docs/components/{ir,lower,interp,rewrite,backend-llvm,backend-cuda}/STATUS.md` | S21 waves as-built; counts corrected (incl. pre-existing drift: interp said 44, rewrite said 41); fixpoint fix + `fill_from` |
| `docs/components/backend-cuda/{IMPLEMENTATION,suggestions}.md` + `plans/plan-iota-fill-stage2.md` | functor rows; plan → SHIPPED with deviations; new 17b headroom row |
| `docs/components/backend-llvm/{STATUS,IMPLEMENTATION,suggestions}.md` | WP3b discipline row; BL1 three-faces accounting; #3 discharged |
| `docs/decisions/ADR-0029…md` | widen Amendment; Status → STAGE 2 SHIPPED |
| `docs/decisions/ADR-0030-backend-plugin-protocol-candidate.md` | NEW (candidate + accepted folder move) |
| `docs/performance/matmul.md` | rewritten (S21 matrix; S20 archived in-doc) |
| `docs/suggestions.md` | llvm row: #3 + WP3b discharged; headroom re-scoped |
| `docs/next-session.md` | rewritten for S22 |
| memory (`model-split-preference`) | codex delegation split |
| this log | written at close (after the final gate), not post-hoc |

## 8. Files changed

Code: `flow-ir` (builder widen/fill_from + Dest threading, graph Widen, validate arm + twins + typing rows, mermaid), `flow-interp` (eval Widen arm; tests/widen.rs), `flow-lower` (diag L1614, lib is_pure_builtin/widen_target, typing, emit emit_widen; golden + rejection tests + snapshot), `flow-rewrite` (replay Widen/Fill-from arms + target threading, equations fold_widen, testgen Widen/Iota/Fill steps + pools; tests widen.rs/testgen_widen.rs/fixpoint.rs), `flow-backend-llvm` (func Widen + WP3b staging discipline; golden tests + 3 re-pins + 2 new), `flow-backend-cuda` (func iota/fill sites + Widen; kernel kernels/twins/TrapCaps; arena membership; golden/differential tests + snapshot). Benches: v2 generators + all regenerated artifacts (incl. 512 set), runner.py/runner.sh 512 legs, `s21_box.sh` (new). Docs: §7. All uncommitted.

**Gotchas recorded for S22 (tail of the standing list):** box clang ≥ 15 (silent llvm-leg skip on 14); back up results.csv before sweeps; codex silent stalls (check file mtimes, not process liveness); box-side nohup survives local crashes (recover, don't re-run); vast.ai `loading` >15 min ⇒ recycle; flow-cli contributes 0 to the workspace test count (bins).

**Next `start` path:** read this log → `docs/next-session.md` → Sapir commits → folder move (S22 P0) → P1 waves.
