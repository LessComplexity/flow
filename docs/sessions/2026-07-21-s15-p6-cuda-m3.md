# Session 15 — 2026-07-18..21 — P6 backend-cuda implementation (M3 reached)

Orchestrator: Kimi Code · workers: sequenced-TDD implementation WPs (WP1–WP6) + a 6-agent parallel phase (loops ∥ differential harness ∥ 4 implementation-review lenses) + fixer · skill: category-architect (standing session contract since S14). Immutable log (ADR-0017). Session scope set by Sapir: "the running machines are unrelated to us, ignore them — go for the cuda backend" (the S14 implementation gate, opened). Late-session note: the subagent quota exhausted during the doc reconcile — the reconcile was completed inline by the orchestrator.

## 0. Continuation brief

Current state: **P6 COMPLETE — M3 REACHED, workspace 673 green** (199+106+139+29+44+27+13+1+115), fmt + clippy clean, docs reconciled bottom-up, nothing live. `flow-backend-cuda` is built and differential-tested on an RTX 4090: 10 examples + 320 closed testgen, raw and rewritten — **640 nvcc compile-and-runs, zero divergences**; traps exit-101; `fmod`/IEEE-÷0 parity; loop-driven matmul `Update`; link tail verified. Both GPU boxes destroyed (~$0.25 total).
Next step: **P7 backend-verilog (M4)** — model-first DESIGN per the ADR-0022 D2 backend-seam practice (feedforward + single-loop FSM only, the E1 standing restriction; the done-protocol handshake). verilator + icarus already installed.
Resume command/check: read `docs/next-session.md`, then `cargo test --workspace` (expect 673).

## 1. Work completed

- **Recovered a mid-air S14**: this session's `start` found S13's docs stale — a parallel S14 session (Kimi Work) had landed the P6 DESIGN + 22-finding design review + ADR candidates uncommitted; Sapir had since ratified ADR-0022 (D2 amended) and accepted ADR-0023/0025. Verified the uncommitted tree green (558) and took the implementation gate from Sapir's word.
- **Implementation (WP1–WP6, sequenced TDD):** crate skeleton + `ty.rs` + `lib.rs` gate (14 tests) → `module.rs` frame + `func.rs` host scalar table (44) → `kernel.rs` (kernels, inline form, BC8 qualifiers; 73) → `loops.rs` guard-first quartet + full golden suite (94) → `tests/differential.rs` full-remote harness port (nvcc discovery, 120 s compile / 15 s run timeouts, exit-101/102 classifier, 13 local tests). Files: `crates/flow-backend-cuda/src/{lib,ty,module,func,kernel,loops}.rs` (~3.9k lines incl. tests) + `tests/{gate,golden_cu,differential}.rs` + 13 insta snapshots.
- **4-lens implementation review (oracle fidelity · CUDA realism · contract coherence · llvm-parity+memory): 2 blockers + 4 majors + 5 minors, all confirmed, all fixed by the fixer (TDD; 115 green).** Then orchestrator line-by-line review of the whole crate (Sapir's standing mandate) — one cosmetic cleanup (WP-era doc labels).
- **GPU leg (DESIGN §6, fresh rental):** one flaky host aborted at 18 min in `loading` (abort-and-record, destroyed); working box `45411610` (RTX 4090, cuda:12.4.1-devel, 12 vCPU, $0.39/hr) — preflight (nvidia-smi + nvcc 12.4.131) → rustup → rsync → `cargo test -p flow-backend-cuda` **green in one pass**: examples raw+rewritten 19.2 s; full suite 89.6 s phase-2 fanned; **320 testgen × raw+rewritten = 640 compile-and-runs, 0 divergences, 0 exit-102s**. Box destroyed per the standing rule.
- **Docs reconciled bottom-up:** backend-cuda DESIGN as-built patches + "As-built (S15)" section (the S14 doc had 2 sections partially patched by the quota-killed agent — completed inline), IMPLEMENTATION.md functor map (stub → full), STATUS.md (design → tested), suggestions.md (headroom); roll-ups: docs/STATUS.md (phase M3, row, capability-matrix cuda column ✅ ×10, session table), docs/IMPLEMENTATION.md (cuda row, loop_plan consumers, 673), architecture-map.md (the first genuine two-site `Loc`/`Trm` pair + checklist refresh), architecture/INDEX.md (cuda modeled·built), next-session.md, plan.md (Phase 2 COMPLETE).

## 2. Decisions

| Decision | Verdict | Why |
| --- | --- | --- |
| Trap-flag encoding collision (S14 design carried it) | **fixed kind+1** (0 quiescent; div_zero ⇒ 1u; index_oob ⇒ 2u; host decodes) | bare-kind made a device div-zero read back as "no trap" — an R1 class cross (101 → 0 + garbage); a 0-store could erase a concurrent OOB trap. The S14 design review missed it; the S15 implementation review caught it (blocker) |
| Return-escape (lower's `Dest::Ret` bypasses `Output`; product/Phi returns) | **epilogue pointer-value guard** — free unless the buffer equals ANY array component of the return value (recursive) | the S14 name-based `Output` removal is unsound for those shapes (use-after-free; the design review's OF-2 recurring at impl level); pointer equality subsumes all escape paths |
| Products-with-arrays in device value contexts | **recorded `Unsupported` cell** (M3) | deep-value support = by-value device products — a DESIGN-level change, not an implementation fix; "never a silent miscompile" (DESIGN §5); corpus-audited: zero hits in 640 emissions |
| Twin-ness through calls | **propagate callerward** (bulk ∨ array-return) | else a `__host__ __device__` fn calls a Twin's host definition — illegal CUDA (the HW-1 class) |
| Sequential WPs 1–3, parallel phase after | sequenced then 6-agent swarm (loops ∥ harness ∥ 4 lenses) | hard module dependencies first; user asked for more parallelism — the rest fanned out cleanly |
| First vast.ai host (18 min in `loading`) | **abort-and-record, rent elsewhere** | the DESIGN's envelope rule; R ≥ 99 reliability for the second (booted <1 min) |
| The 3 pre-existing vast.ai instances | **ignored — not used, not destroyed** | Sapir: "unrelated to us"; the DESIGN's fresh-rental fallback covered the leg |
| Subagent quota exhausted mid-reconcile | **reconcile completed inline** | doc work doesn't need the swarm; no quality compromise (orchestrator-authored) |

## 3. Tests, checks, benchmarks

| Check | Result | What it proved |
| --- | --- | --- |
| `cargo test --workspace` | **673 passed, 0 failed** (was 558 at S14 close) | the increment + no regressions |
| `cargo test -p flow-backend-cuda` (local) | 115 passed (87 lib + 13 differential + 4 gate + 11 golden); remote legs skip-with-reason | local gate; honest skips |
| `cargo test -p flow-backend-cuda` (on the 4090) | **115 passed — M3** | the ADR-0020 §4 duty discharged |
| differential: 10 examples raw+rewritten | green (19.2 s) | first nvcc contact clean |
| differential: testgen closed sweep | **320 draws (256+64), ≥256 non-diverged, raw+rewritten — 640 compile-and-runs, 0 divergences** (phase-2 88.6 s fanned) | random-program oracle equality on GPU |
| differential: pins | two sequential loops; exit-only-payload-once (text pin, raw+rewritten); loop-driven matmul `Update`; traps exit-101 (div-zero, `Update` OOB); float-print parity (f64 `0.30000000000000004`, f32, IEEE ÷0, **`fmod`** — the DESIGN §10 question answered green) | the ported llvm-suite classes |
| clippy `--all-targets` / fmt | 0 warnings / clean | gate |
| vast.ai cost | ≈ $0.25 total (aborted ~0.33 h × $0.3611 + working ~0.35 h × $0.39 — estimate; console invoice is the cross-check) | accounting, not performance |

## 4. Live handoff state

| Type | Handle / location | State | Inspect / resume | Stop / cleanup |
| --- | --- | --- | --- | --- |
| branch | `main` | **uncommitted** (S14 + S15 work; the session process owns commits) | `git status` | none |
| machine | vast.ai 45411610 + 45410522 (P6 boxes) | **destroyed** | `vastai show instances` | done |
| machine | vast.ai 45170851/45170852/45181070 | running — **Sapir-declared unrelated to Flow (S15): not used, not destroyed, not Flow state** | n/a | not ours |
| toolchain | clang/verilator/icarus local; nvcc remote-only | present | `which clang verilator iverilog` | keep |
| artifact | 13 golden `.cu` snapshots | `crates/flow-backend-cuda/tests/snapshots/` | `cargo test -p flow-backend-cuda --test golden_cu` | keep |

## 5. Open items

| Priority | Item | Doc/code reference | Next action | Done when |
| --- | --- | --- | --- | --- |
| P0 (S16) | P7 backend-verilog (M4) | `components/backend-verilog/STATUS.md` (stub) + HANDOFF §4.3 (E1 restriction) | model-first DESIGN (done-protocol handshake; feedforward + single-loop FSM only) → adversarial review → implementation | the M4 line green on verilator |
| P1 | Decide **ADR-0024** (templates) | `decisions/ADR-0024-templates-candidate.md` (Q1–Q6) | Sapir answers | status flips |
| P1 | Write the **coproducts ADR candidate** on Sapir's word (ADR-0023's checked-allocation dependency) | `decisions/ADR-0026-coproducts-sums-candidate.md` (S14 seed) | orchestrator authors; Sapir decides | ADR exists |
| P2 | **interp `read-before-write` panic on the exit-only-payload shape** (S15 finding, pre-existing) | `flow-interp/src/eval.rs:297` (via `stage_pair`, decide cone); why llvm's pin is text-only | reproduce standalone; fix or record as a documented limitation | pinned one way or the other |
| P2 | rewrite `is_canonical`/`exit_of` → `flow_ir::loop_plan` (S12 P3) | `crates/flow-rewrite/src/plan.rs` | mechanical swap | one predicate, four consumers |
| P2 | Lower rejects array-typed loop-exit bindings in map/fold bodies (L1201) — emitter-ready, unreachable | `flow-lower` + backend-cuda STATUS "upstream note" | revisit if the shape ever becomes legal | lower change + twin-path differential |
| P3 | Parallel float `Fold` oracle re-pin — ADR candidate for Sapir | backend-cuda DESIGN §1/BC4/§10 | Sapir's call | ADR exists or declined |
| P3 | Doc leftovers | backend-llvm DESIGN §0/§8 + suggestions #4 (stale `-O2`-as-future); `docs/notes/` index; heavy/fast test split | small edits, any session | reconciled |
| P3 | Lower §16 OQ1–OQ8 | lower/DESIGN §16 | Sapir answers individually | closed |

## 6. Architecture / model changes

New component **built**: `backend-cuda` — the project's first genuine two-site execution pair (`Loc`: host process + GPU device; `Trm`: the enumerated `cudaMemcpy` H↔D crossings, cost real and counted — DESIGN §2's inventory; zero whole-array D→H by construction). `Operation` set unchanged; three recorded `Unsupported` cells (nested loops — the shared ceiling; arrays-in-products on device; per-thread locals > 16 KiB). `flow_ir::loop_plan` gains its fourth consumer. ADR-0022 D2's backend-seam practice paid a second time (the DESIGN's categorical section) — and the S15 review's trap-encoding catch is the first Level-B-relevant *implementation* finding at a backend seam (recorded in the DESIGN's As-built section; no D2 re-evaluation triggered — the amendment's scope stands). Architecture-map §4.5: all six laws PASS with the real pair.

## 7. Docs reconciled

| Doc | Change |
| --- | --- |
| `components/backend-cuda/DESIGN.md` | header → implemented+M3; §3/Dat kind+1; §5 → three cells; §1/§9 BC8 as-built (split base case, callerward propagation); §2 ownership amendments (pointer-value guard; caller-frees honest note); §6.1 executed (fresh rental, aborted box, cost); §6.6 link tail verified; §7 headroom (split built; by-value products added); §10 questions closed; **§"As-built (S15)"** added |
| `components/backend-cuda/IMPLEMENTATION.md` | stub → full functor map (Dat/Trn/Trm/rules/cells/bridges → `file:symbol`) |
| `components/backend-cuda/STATUS.md` | design → **tested** (115 ✅; M3 evidence; 3 cells; honest notes) |
| `components/backend-cuda/suggestions.md` | 10 headroom rows + parked/built distinctions |
| `docs/STATUS.md` | phase M3; backend-cuda row tested 115 ✅; capability-matrix cuda column ✅ ×10; session table S15 row |
| `docs/IMPLEMENTATION.md` | cuda row built/tested; `loop_plan` consumers +cuda; 673 green |
| `docs/architecture-map.md` | first genuine two-site `Loc`/`Trm` (cuda) — atoms, components, checklist preamble refreshed |
| `docs/architecture/INDEX.md` | cuda modeled · built/tested (S15, M3); planned-rows footnote corrected |
| `docs/next-session.md` | rewritten for S16 (P7 first) |
| `plan.md` | Phase 2 → COMPLETE (M3 reached) |

## 8. Files changed

`crates/flow-backend-cuda/**` (new crate: 6 src files ~3.9k lines + 3 test files + 13 snapshots) · `Cargo.lock` (dep edges) · the docs in §7 · committed? **No — the session process (Sapir) owns commits**; tree state = S14's uncommitted set + S15's crate + reconciled docs.

**Next `start` path:** read `sessions/2026-07-21-s15-p6-cuda-m3.md` (this log) → `docs/next-session.md` → P7 open item → `cargo test --workspace` (expect 673).
