# backend-llvm — suggestions (category-theory derived)

> Improvements deduced by FRAMEWORK rules. Each cites its rule and names the concrete
> change. Not applied — a backlog for future work.

| # | Rule (§) | Smell found | Proposed change | Payoff |
| --- | --- | --- | --- | --- |
| 1 | §4.4 / §7.4 strategy 2-category | Three backend crates will realise one contract `CategoryIr → TargetText`; no shared contract is declared yet | Fix a shared `Backend` trait + `TargetText` type by ADR **before** the first backend is written (the firm candidate in [categorical-model.md §7.5](../../architecture/categorical-model.md)) | Adding a target = adjoining an object; never edits the core |
| 2 | DESIGN §2 `Update` row + BL1 | `Update` always `llvm.memcpy`s the whole source array into a fresh alloca before the element store — the naive-copy semantics ADR-0021 records as replaceable | In-place lowering via a last-use analysis: when the source array is dead after the `Update` (its only consumer is the rebind), skip the memcpy and store into the source slot | Lifts the O(n)-per-update copy **and** the alloca-slot stack ceiling (BL1) that caps the perf shape |
| ~~3~~ | DESIGN §4 perf + BL1 | ~~Array literals are N `Pair` stores, so the perf module is ~1M lines at N = 262144 and clang `-O2` blows past 25 min CPU — top N capped at 65536 (as-built S13)~~ **APPLIED S21 via ADR-0029**: `iota`/`fill` + `widen` make large arrays O(1) IR (procedural bench sources 3.8 MB → 72 KB; `.ll` 23 MB → 21 KB); WP3b killed the residual optimizer blowup (aggregate staging → ptr/memcpy; matmul256/512 clang -O2 = 0.08 s / 57 MB) | — | **CLOSED S29 — the heap-lowering half landed too** (plan-s29 item 4, DESIGN BL9): `func.rs:entry_alloc` places an entry-block block of ≥ `HEAP_MIN_BYTES` = 256 KB in the `flow_rt_alloc` arena, freed by one `flow_rt_free_all` before the entry fn's `ret`; `matmul2048_cap_f32` runs locally for the first time (was SIGSEGV on the 64 MB macOS stack). Ceiling recorded, not closed: entry function only — a big array local to a Named fn or a Map/Fold body keeps its `alloca` until `flow_rt_free(ptr)` + `LastUsePlan` free points exist. The sepia-literal perf point still awaits a procedural sepia source (generator work, not backend) |
| 4 | DESIGN §8 open question | Differentials run only at `-O0`; LLVM-level UB the emitter accidentally relies on would not surface | Add a `-O2` differential row over the same example + testgen pool (the harness already compiles at both opt levels for perf) | Catches miscompiles that only appear under optimization; cheap once `-O0` is green |
| 5 | DESIGN §2 Index (as-built S13) | `guard_index` emits a two-sided signed compare for every index type; the `slt 0` half is provably dead for a zero-extended u8 index | Drop the lower-bound compare on the u8 path (emit `uge` only, matching §2's original spec) once a golden pins the u8 guard shape | Two fewer instructions per u8 `Index`/`Update`; the `.ll` matches the spec text exactly |
| 6 | §5 deduce-don't-store / ADR-0027 realization | **By-value array captures in body-input products (measured S18):** a captured array packs inline into the body fn's argument struct — the capture-form matmul's fold body takes `{ [4096 x double], i32, [4096 x double], i32, double, i32 }` = **64 KB per call × 262,144 calls ≈ 17 GB of memcpy** (2,076 ms at N=64, 6.6× SLOWER than the loop form's 316 ms). The CUDA backend's pointer handles make the same program 277 ms | Pass arrays **by reference** in body-input products (pointer-typed components for array captures; read-only semantics preserved, so observably identical; the interp oracle is unaffected — it reads values, not representations) | The capture form becomes what it should be on llvm: a normal native GEMM, faster than the loop form everywhere |
| 7 | DESIGN §8 perf headroom + §5.5 | Internal fns emit with **zero attributes**, so LLVM must assume every `internal` fn may write arbitrary memory, unwind, and never return — blocking load elimination / argmem reasoning across the per-element body calls (and across Named calls once #8 lands) | A conservative syntactic capability set per fn (no integer `Div`/`Mod`, no `Index`/`Update`, no `Print`/token, and transitively-clean callees — the `TrapCaps` fixpoint spirit): clean fns get `readonly nounwind willreturn`, their by-ref `ptr` params `noalias nocapture readonly`; trap-capable/printing fns get nothing | LLVM optimizes across call boundaries with the same facts the emitter already knows; trap/print behavior is untouched by construction |
| 8 | BL5 internal ABI + ADR-0027 realization | **By-value array call arguments (measured S19):** a Named call packs array arguments inline — the loop-form matmul's per-element `cell(a, b, i, j)` copies both N² arrays per call (the bulk of the loop form's 316 ms at N=64) | Pass top-level array (components of) call arguments **by reference** — array parameters are observably read-only (Flow value semantics; functional `Update` copies to a fresh alloca) — reusing the #6 by-ref machinery per-signature so every call site agrees with the callee | No array bytes cross any call boundary; the loop form reaches the capture form's native-GEMM class |
| 10 | plan-minimal-emission §0 (the S22 mandate) + DESIGN §4 | **WP-E assessment (measured S23):** every object routes through an entry-block alloca + store/load ceremony — `matmul512_cap.ll` = 539 non-blank lines / 75 allocas (`fn3` alone 23). LLVM IR has no expression nesting, so `emission_plan`'s Inline class buys zero textual collapse there (every op is one SSA line regardless); the analogous win is alloca→direct-SSA value plumbing, a dominance/Phi-discipline emitter redesign (the allocas exist precisely to dodge SSA dominance at guards/loops) | If ever taken: full SSA-direct emission, or the narrow slice (skip allocas for Dissolved products only). Measured headroom: `opt -passes=sroa` alone takes the exhibit **539 → 181 lines, 75 → 5 allocas (−66% / −93%)** — the entire shuffle is mechanically recoverable and clang `-O2` already runs it (S21: `-O2` runtime beats single-thread C++ at 256/512) | **Deferred — S23 verdict:** `-O0` text honesty is the only stake; runtime is already optimal; a Phi-discipline rewrite does not pay while the matrix is the done-bar |
| ~~9~~ | S20 `bounds_proof` (ir BL7) + DESIGN §2 Index | ~~The one-kernel matmul's fold-body guards survive wave 4~~ **CLOSED S25 — found already shipped:** the S20c capture-range recursion (`flow-ir/src/algo.rs` `bounds_proof_inner` capture-threading + `element_range`/`capture_proj_of_input`) already proves the matmul fold-body chain; S25 verified current emission has ZERO fold-body guards (6 `icmp` = loop conditions only, raw AND rewritten) and pinned it at the query level (`bounds_matmul_fold_body_proven_through_captures`, tests/algos.rs) | — | The premise of s24.md reading 6 ("two guarded checks per inner iteration") was stale — the ≤512 CPU gap re-attributed S25 to spawn floor + oversubscription; the *vectorization* unlock turned out to be tiling (plan-tile-emission.md), since the per-cell fold is a strict recurrence no guard removal can vectorize |
| 11 | tile-ladder-direction §"The recorded facts ARE the optimization menu" (a matched coefficient = reuse the graph can see) + §4.4 strategy rungs | The conv micro-kernel is TI=1 (plan-s28 recorded ceiling): every output row re-loads its img window rows although `b.ci == cq` — img row `i+1` serves output rows `i..i+2`, reuse ×3 left on the table | TI over output rows in `emit_tiled_map_conv` — the rung-2 i-blocking discipline transplanted to the conv nest: per (i-block, j-tile) one unrolled tap nest feeds TI acc vectors | img-read traffic ÷TI; the conv kernel's remaining 1t gap closes without a new site type |
| 12 | §3 consolidation + placement (`DataLoc`): change the placement so the record becomes matmul-shaped — never a new site type (plan-s28 ceilings) | conv rides a dedicated TI=1 micro-kernel while the matmul rungs (TI blocking, packing, k-unroll/prefetch) sit one placement away, unreachable for k-split sites (`packing_site` refuses them) | **im2col** as a `DataLoc` sibling of `emit_pack_copy`: repack the img window into patch-major panels so the site records as matmul-shaped | rungs 2+3 (and every future matmul rung) consume conv unchanged — one pack, the whole ladder |
| 13 | the same zero-coefficient reuse fact one level down — shuffle-level fanout (plan-s28 ceilings) | the FIR window kernel re-loads `b[k + jb + r·TJ + lane]` from L1 at every k although consecutive k overlap the window by TJ−1 lanes; clang CSE of L1 reloads is the v1 approximation | FIR window **register rotation**: shift the resident window vector across k instead of re-loading (beyond what the emitter expresses today) | b loads ÷K on the window path; the FIR rung approaches the roofline |
| ~~14~~ | placement honesty (`Loc`/`Trm`): the two legs' `Trm` boundaries differ — the bracket must span what the baseline spans (S28 measured finding) | the shapes LEGS time flow's data-gen maps while the cpp/rust/numpy baselines time kernel-only: conv2d legs 0.5109 par / 0.427 1t vs kernel-only ≈ 0.02-0.05 / ≈ 0.07 — a kernel 3-6× WIN reads as a leg-level loss (the untiled 264k-cell img-gen map dominates the bracket) | gen-map fusion/inlining (the gen map is a pure producer — fold it into the consumer's emission) OR a kernel-scoped perf bracket in the shapes harness | **CLOSED S29 — fixed by the `time` builtin, not by fusion.** `benches/shapes/*.flow` open their bracket after generation and print `iter ms=`; `--perf` is gone from `shapes_ab.sh`. Measured consequence: the S28 estimate for the conv2d 512 kernel (≈0.04 ms, "3× over cpp-mt") was a subtraction, not a measurement — the real kernel is 0.083 conf / 0.107 fma, ahead of cpp-mt 0.112 by 1.3×. `benches/matmul/` is NOT migrated (recorded, s29.md §5) |
| 15 | per-target factors are policy owned per site, not doctrine — tile-ladder-direction §"Per-backend width" (the runtime owns slicing, not the emitter) | `GRAIN=4096` is a flow-rt compile-time constant: fir_65536 slices into 16 tasks on a 14-thread pool ≈ 1.14 waves — the tail wave runs mostly idle at sub-ms N (S28 campaign) | GRAIN adaptive to N and pool width in flow-rt (runtime policy; emitter sites unchanged) | removes the sub-ms tail quantization on small-N par legs |
| 16 | §4.2 placement is a span — a nest order is a `TrnLoc`, and which one pays is a MEASUREMENT, not a derivation (S29) | the KC k-panel nest (jc, kc, ic) was designed on A-re-read traffic alone (C/TJ → C/NC, ~250 MB saved at 1024 f32) and priced no counterweight; measured, it is **3× SLOWER** at 1024 f32 (fma 59.8 ms on / 19.8 off) because the order forces partial sums to park in `out` and reload once per k-panel | shipped default-OFF (`EmitOpts::kc_nest`), bit-exact either way. Either measure it where it was designed for — box zen3 at 4096, 16 GB of A traffic — or take the parking-free variant recorded in plan-s29 Ceilings (jb → ib → kc → jt, `TI×NC` acc, one `out` write, at the cost of a K×NC B working set) | a rung that pays where it was designed to, or a recorded dead end with the number that killed it |
| 17 | the same matched-coefficient reuse fact as #11, now MEASURED rather than predicted (S29) | conv2d beats cpp-mt at 512 (0.083 vs 0.112) and loses **3.4×** at 1024 (0.445 vs 0.133) — cpp gains superlinearly across the size step, Flow degrades: the TI=1 kernel re-loads every img window row per output row, and that term takes over once the working set stops fitting | #11 (TI over output rows) is no longer speculative — it is the conv shape's measured blocker; #12 (im2col) is the alternative that reaches the whole matmul ladder instead of one rung | the shape's 1024 leg stops being a loss; the ladder's "every shape wins at scale" claim survives its own benchmark |

_(Suggestion 2 — last-use `Update` memcpy elision — **applied 2026-07-22** (optimization marathon wave 3): the backend-llvm consumer of flow-ir's `last_use_plan` query ([../ir/plans/plan-last-use.md](../ir/plans/plan-last-use.md) §2 rule 4). `src/func.rs:FnEmit::update_in_place_source` is the one legality test — the plan proves the `Update`'s source array `dead_after` the update (every use ranked at/before it under rule 1's decide < `LoopExit` < advance < `LoopBack`, ¬escapes, ¬carried) — and `src/func.rs:FnEmit::{lup, elided_updates}` pre-pass the verdict so an elided target mints no entry-block alloca. `emit_update` then skips the whole-array `llvm.memcpy` and the target SHARES the source's slot: the element store lands in place (in the loop-carried matmul4 shape, straight into the merge's own storage — the back-edge copy becomes an identity, no double-buffer assumptions touched). A ptr-resident (by-ref, borrowed) source is vetoed explicitly — the write would land in caller memory (the plan's rule 2 already marks borrowed inputs escaping; the check is the emitted-text-level second line) — and everything the plan can't prove keeps the fresh-alloca copy, byte-identical to before. Structural pins: `golden_update_inplace_carried_loop` (the matmul4-class loop form emits NO `llvm.memcpy` call) and `golden_update_memcpy_kept_when_not_dead` (escape via a `Pair` field of the output, and a by-ref source, both keep the copy). matmul4/16/64 emit+compile+run oracle-equal at `-O0`/`-O2` (`-275`/`3748`, `1815`/`6944`, `1047`/`2107`); matmul64 native `-O2` stays at the 0.01 s floor (already floor-bound after #8 — the elision's pay here is structural: the per-iteration 16 KB memcpy is gone from the `.ll`, one of the loop's aggregate copies; the remaining merge/back-route copies are the loop driver's, not this row's).)_

_(Suggestion 6 — by-reference array captures in body-input products — **applied 2026-07-21** (optimization marathon wave): the first-k Array components of a Map/Fold body input lower to `ptr` — `src/ty.rs:lower_body_input_ty`; `src/func.rs:FnEmit::{byref, ptr_resident}` state, `array_operand_ptr` (Index/Update/Map/Fold read through the forwarded pointer), `body_call_arg` (scratch fields carry addresses), escaping uses deep-copy through the pointer in `load_whole`. The capture-form matmul at N=64 went **2.08 s → 0.01 s** native `-O2` (min-of-3), oracle-equal at `-O0`/`-O2`; golden `capture_one_kernel_matmul` re-pinned to the ptr shape.)_

_(Suggestion 7 — truthful fn attributes — **applied 2026-07-21** (optimization marathon wave 2): `src/func.rs:FnAttrs` — the clean-set and loopy fixpoints over `Call` + `Map`/`Fold` body edges, in the `TrapCaps` spirit. Clean fns emit `readonly nounwind`, plus `willreturn` when the transitive closure has no `LoopEnter` and no call/body cycle (a `willreturn` violation is UB, so a possibly-diverging closure must never carry it); a clean fn's bare-`ptr` by-ref parameter additionally gets `noalias nocapture readonly`; unclean fns stay bare. `flow_trap` is declared `noreturn` (flow-rt defines it `-> !`); print externs stay attribute-free. The classic `readonly` spelling verifies under the local clang 22. The full differential (320 closed testgen cases × raw+rewritten × `-O0`/`-O2`) stays green.)_

_(Suggestion 8 — by-reference array call arguments (BL5 amendment) — **applied 2026-07-21** (optimization marathon wave 2): a Named fn's top-level Array input (the input itself, or direct product components) lowers to `ptr` — `src/ty.rs:lower_named_input_ty` (the capture lowering with `k = u32::MAX`); `src/func.rs:emit_call` assembles the argument with array addresses (forwarding ptr-resident feeders) via the `body_call_arg` template; the callee's array parameters become ptr-resident through the #6 machinery (`FnEmit::byref` with `k = u32::MAX`; a bare-Array input is itself ptr-resident). `array_operand_ptr` learned the by-ref-input-field case and `Zip`/`Enumerate`/`body_call_arg` route through it; `load_whole` assembles the by-value whole of the by-ref input product for escaping uses (`Pair`/`Output`). Loop-form matmul64 **0.33 s → 0.01 s** native `-O2` (min-of-3), oracle-equal (`1047`/`2107` at N=64; `1815`/`6944` at N=16, both opt levels); the capture form stays at the 0.01 s floor. Scalar-only arguments emit byte-identical text to before; nested products-in-products stay by value (recorded limitation)._

_(S20 `bounds_proof` guard elision + the FnAttrs proof refinement — **applied 2026-07-22** (optimization marathon wave 4): the backend-llvm consumer of flow-ir's `bounds_proof` query (the S20 kernel-gap analysis). `src/func.rs:FnEmit::bp` computes the plan once per fn next to `lup`; `emit_index(m, …)` drops the two-sided guard (`icmp slt`/`icmp sge`/`or` + the `flow_trap(1)` block) when the plan proves the index statically inside `[0, n)` — a proven `Index` can never fire, so trap-freedom is exact — and emits just the GEP+load. Everything unproven emits today's guard byte-identical (`Update` keeps its guard regardless — conservative this wave). The `FnAttrs` clean set learned the same fact: a PROVEN `Index` is not trap-capable, so a fn whose only trap-looking ops are proven indexes now carries `readonly nounwind` (+ `willreturn`), and a clean bare-`ptr` by-ref parameter `noalias nocapture readonly` (the `willreturn` honesty argument: with the guard elided there is no trap path at all). Structural pins: `golden_proven_index_guard_elision` (the matmul-cell class — an enumerate'd map body with the affine `h*2+r` index emits ZERO `icmp slt`/`sge` guard instructions while the signed `Div`/`Mod` `icmp eq` guards stay; the unproven sibling — index by an unknown i32 parameter — keeps exactly one guard + one trap) and `golden_proven_index_fn_clean_attrs` (the enumerate-`.0` cell body is attribute-clean with zero `icmp`; a bare-Array Named fn over constant indices carries `ptr noalias nocapture readonly` + `readonly nounwind willreturn`). Re-pinned snapshots: `vector_add`/`zip_demo`/`capture_map`/`update_inplace_carried_loop`/`capture_one_kernel_matmul` lose only their constant result-index guards in `flow_main` (`c[0]`/`c[15]`/`b[1]`/`r[2]`) — fir's loop-carried `coeffs[k]`/`signal[4+k]` guards stay, as does the one-kernel matmul's fold body (suggestion #9). matmul4/64_cap emit+compile+run oracle-equal at `-O2` (`-275`/`3748`, `1047`/`2107`); matmul64_cap native `-O2` min-of-3 **11.72 ms → 11.34 ms** (process-spawn floor-bound — the hot fold body is unchanged this wave; the znver2 re-measure is a later box wave). The full differential (320 closed testgen cases × raw+rewritten × `-O0`/`-O2`) stays zero-divergence.)_

## Detail
### 1. Backend strategy 2-category
Already derived and adversarially verified in the Session-06 audit — see
[categorical-model.md §7.5](../../architecture/categorical-model.md) item 1. Owned by a
future backend ADR; recorded here so the component increment starts from it.

### 2. In-place Update via last-use
ADR-0021 makes naive copy the semantics everywhere and names in-place elision via
last-use as recorded headroom; DESIGN §2's `Update` row repeats it. The copy is the
only reason `Update` needs a distinct target alloca, so eliding it on a dead source
also collapses the two allocas into one — the direct lever on the BL1 stack ceiling
that `perf_baseline.rs:run_big_stack` currently works around with `ulimit`.

### 3. Array-fill primitive / heap lowering — CLOSED S29
The perf baseline's own escape hatch (as-built S13). Both suggestion 2 and this one
attack BL1 from opposite ends — 2 shrinks per-op frame growth, 3 shrinks the initial
literal — and either alone restores the DESIGN N = 262144 point. **Both halves are
now built:** the array-fill half by ADR-0029 (`iota`/`fill`/`widen`, S21) and the
heap half by S29 (`HEAP_MIN_BYTES`-gated `entry_alloc` + the flow-rt arena, DESIGN
BL9). What survives is a narrower row, recorded in BL9 rather than here: heap
placement is entry-function only, so an unbounded-call-count fn's big local still
lives on the stack until the emitter computes per-allocation free points.

### 4. `-O2` differential row
DESIGN §8's first open question; the perf harness already produces `-O2` binaries, so
this is a harness wiring change, not new emission code.

### 5. Dead u8 lower-bound compare
Cosmetic/spec-fidelity: the two-sided guard is correct but `guard_index` (as-built S13)
diverges from §2's uge-only u8 text. A one-line branch on the index signedness restores
the spec shape; gate it behind a golden so the u8 guard `.ll` is pinned first.

### 9. Inter-procedural body quantification (the fold-body guards) — CLOSED S25

**S25 verdict: already shipped by S20c** (the capture-range recursion this section asks
for exists at `bounds_proof_inner`'s capture-threading step); emission verified
guard-free and the proof pinned (`bounds_matmul_fold_body_proven_through_captures`).
The text below is the original (stale) finding, kept for the record:
Found while discharging the S20 consumer row (wave 4): `bounds_proof`'s Map/Fold body
seed resolves the site source's element range only in the fn where the site lives
(`algo.rs:element_range` sees iota / enumerate / literal-ramp definers). In the
one-kernel matmul the fold's ramp array is a *capture* of the enclosing map body (a
by-ref Proj at the fold site ⇒ `element_range` → None), and the body's captured `i`/`j`
are parameters with no range — so `a[i*N+k]`/`b[k*N+j]` keep their per-iteration
guards even though the whole chain is statically affine. The fix is ir-side: follow a
by-ref captured array back through the unique body site to the caller's defining op,
and quantify body captures that are themselves derived from the enclosing site's
element. The llvm consumer needs no change — `emit_index` already keys on
`bp.proven(m)`, so new proofs drop guards automatically.
