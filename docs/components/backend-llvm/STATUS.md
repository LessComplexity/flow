# Component: backend-llvm

Status: tested
Last updated: 2026-07-23 · **S25 — tile emission v1 (plan-tile-emission, session-directive): recognized map{fold} sites emit the `TILE_J=16` k-outer/lane-inner register micro-kernel (`emit_tiled_map`) instead of per-element body calls — per-cell op/operand/k-order preserved exactly, stdout byte-equal at every thread count and opt level (tile-vs-untile + oracle differential at -O0/-O2, incl. the 1-D FIR case); `EmitOpts{tiling: true default, perf_timing}` + `emit_with_opts` (default byte-identical pre-options), `--perf`/`--no-tile` example flags; FLOW_PERF compute timer brackets `flow_main` (perf-only extern decls + attr drop); range clipping serves sequential and Split-task flavors identically; 40 tests (16 differential incl. the 1280-run sweep + 24 golden). Measured local: tile-vs-untile 2.5–4.6× 1t (matmul/attn), NEON 4-lane confirmed in disasm; the untiled per-cell fold is a strict recurrence — tiling is the only bit-exact SIMD route.** Previous: **S24 — parallel orchestrator v1 (plan-parallel-orchestrator, Sapir-ratified): `flow_main` consumes `flow_ir::path_plan` — execution-graph paths become scheduler tasks (frame struct + `@task{i}` fns + static rank/dep table), bulk sites slice across cores, the speculate-and-order trap protocol keeps output byte-equal to the oracle at any thread count (R-PAR). flow-rt gains the work-stealing pool (`flow_par_*`, 11 externs). Two orchestrator review finds fixed pre-merge: pinned-task pool race (registration-time `flow_par_pin`), effectful-loop seed race (pre-`LoopEnter` checkpoint hoist).** Previous: S21 WP3b — first-class aggregate array moves eliminated (the BL1 optimizer face: staging/Pair/Proj/Phi/Output/loop-route array movement is now direct-pointer or `llvm.memcpy`, never a whole-array SSA value; matmul256_cap clang -O2: OOM-kill → **0.08 s / 57 MB**) + S21 `Widen` emission (ADR-0029 amendment: `sext`/`sitofp`/`fpext`). S20: proven-`Index` guard elision + `FnAttrs` proof refinement; last-use Update elision (#2); truthful fn attributes + by-reference captures/call args (#6/#7/#8)
Spec references: ADR-0020 (backend emission contract) > category-ir.md §8.1 (`F_LLVM`) + §8.5 (piecewise correctness). Binding model: DESIGN.md (L1–L4, §2–§4, BL1–BL8). Supporting: ADR-0016 (guard-first loops), ADR-0021 (Update), architecture.md §4.1.
Depends on: ir, interp (dev), rewrite (dev), flow-rt Depended on by: cli

## What works

**P5/M2 complete (S13).** Text emitter `emit(&CategoryIr) -> Result<String, EmitError>` (`src/lib.rs`) realizing `F_LLVM` as a `.ll` String, plus the shared `crates/flow-rt` runtime (7 print externs + `flow_trap` exit-101) it links against.

- **Parallel orchestrator v1 (S24; `plans/plan-parallel-orchestrator.md`, ratified)** — a multi-task `flow_main` emits the parallel form: one `%Frame` struct holds every object slot (task fns GEP off `ptr %frame`; update-elision aliases share fields); one `define internal void @task{i}(i64 %lo, i64 %hi, ptr %frame)` per `path_plan` task (Split = the bulk element loop over `%lo..%hi` via `bulk_bounds`; Seq = the chain/fold/pure-loop morphisms verbatim); the host spine registers the static table (`flow_par_begin/task/pin/dep/launch` with compile-time ranks and deps), interleaves token ops with checkpoint `flow_par_wait`+`flow_par_check` pairs (packed `(task<<32)|threshold` constant arrays; threshold = watermark, `u32::MAX` = completion), executes pinned tasks inline via `flow_par_run_pinned` after a dep-wait+check, and closes with `flow_par_finish`. **Speculate-and-order guards** (plan §2 rule 3): task/body-fn trap arms call `flow_par_trap(site_topo, kind)` and continue with a zero dummy — branch-around keeps `sdiv`/`srem`/loads off the bad path (no UB), Update skips only the element store; scalar chain tasks publish `flow_par_watermark` after each guarded site; `bounds_proof`-proven elisions and the constant-divisor credit are unchanged. Body fns of parallel sites emit the task flavor via the site-topo map (`lib.rs:parallel_body_sites`) and drop `readonly` (`FnEmit::runtime_write`). Single-path fns and every non-entry fn emit today's sequential text byte-identical (v1 scope: `flow_main` only — recorded ceiling). flow-rt: global work-stealing pool (rank-seeded per-worker deques, help-first waits, `FLOW_PAR` env, `GRAIN=4096` slices, CAS-min trap flag, per-task watermarks, registration-time pinning). R-PAR is pinned live: trap-mid-parallel stdout-prefix parity, `FLOW_PAR=1`/`8`/unset byte-equality, run-twice equality, big-N split parity — all at `-O0`+`-O2`.

- **Op table (§2)** — every Core op emits (`func.rs`): wrapping int `add/sub/mul` (no `nsw`/`nuw`); signed `Div`/`Mod` with zero-guard + `MIN/-1` guard (Div ⇒ `MIN`, Mod ⇒ `0`, `wrapping_div`/`wrapping_rem` parity); u8 `udiv`/`urem`; IEEE floats at width (`fadd`..`frem`, `fneg`); int/float compares (`fcmp` ordered, `une` for `!=`); `select`-Phi (strict, both cones execute — BL2); `Pair`/`Proj` as GEP store/load; `Call` direct; counted-loop `Map`/`Fold`/`Zip`/`Enumerate`; `Print` via flow-rt at its topo position.
- **Loop CFG (§3)** on `flow_ir::loop_plan` (BL7 — the one-source-of-truth per-merge attribution predicate; `loops.rs:emit_loop`): guard-first quartet (`entry`/`header`/`advance`/`exit`/`after`), decide cone every iteration incl. the exit one, advance cone unreachable on exit. Two sequential loops per fn work; an exit-only computed payload is emitted exactly once (driver-owned, `walk()`).
- **Token erasure (L4)** — arity-0 (no slot), arity-1 (bare component, plain store/load), arity-≥2 (`{…}` struct + GEP), remap derived on demand from the ty (`ty.rs`).
- **`Update` (ADR-0021)** — same index guard as `Index`; the whole-array `llvm.memcpy` into a fresh target slot fires only when flow-ir's `last_use_plan` cannot prove the source dead (suggestion #2, applied 2026-07-22 — the plan-last-use §2 rule 4 consumer): when the source array is `dead_after` the update (uses ranked decide < `LoopExit` < advance < `LoopBack`, ¬escapes, ¬carried — the loop-carried matmul4 shape), the memcpy is skipped and the target shares the source's slot, the element store landing in place in the merge's own storage (`func.rs:emit_update`; legality `func.rs:FnEmit::update_in_place_source`; pre-pass `FnEmit::{lup, elided_updates}` so an elided target mints no alloca). Ptr-resident (by-ref/borrowed) sources are vetoed explicitly — never written in place.
- **By-reference array captures (ADR-0027; suggestion #6, applied 2026-07-21)** — a Map/Fold body fn's first-k Array input components lower to `ptr` (`ty.rs:lower_body_input_ty`; k from the unique Map/Fold site, `func.rs:FnEmit::body_captures`). A captured-array Proj lands in an `alloca ptr` holding the forwarded pointer (`FnEmit::ptr_resident`); `Index`/`Update`/`Map`/`Fold` array operands read through it (`func.rs:array_operand_ptr`); the body call's scratch carries addresses, not array bytes (`func.rs:body_call_arg` — the transitive fold-in-map case forwards the loaded pointer). Escaping uses (`Pair` into ordinary products, `Call`, `Output`) deep-copy through the pointer in `load_whole` — observably identical to the inline value (read-only capture semantics; the interp oracle is representation-blind). k=0 bodies and scalar captures emit byte-identical text to before.
- **By-reference array call args (BL5 amendment; suggestion #8, applied 2026-07-21)** — a Named fn's top-level Array input (the input itself, or direct product components) lowers to `ptr` (`ty.rs:lower_named_input_ty` — the capture lowering with `k = u32::MAX`; nested products-in-products stay by value, a recorded limitation). The callee's array parameters ride the same ptr-resident machinery as captures (`FnEmit::byref` with `k = u32::MAX`; a bare-Array input is itself ptr-resident, its slot an `alloca ptr`); `func.rs:emit_call` assembles the argument through the `body_call_arg` template — array components store their address (forwarding ptr-resident feeders), scalars their value — so all call sites (host paths and body fns) agree with the callee per-signature by construction. `array_operand_ptr` also covers an op reading an array component straight off the by-ref input product (a `load ptr` from the field), and `Zip`/`Enumerate` route through it; `load_whole` assembles the by-value whole of the by-ref input product for escaping uses. Scalar-only arguments emit byte-identical text to before.
- **Truthful fn attributes (suggestion #7, applied 2026-07-21; proof-refined 2026-07-22, wave 4)** — `func.rs:FnAttrs` computes the conservative syntactic capability set per fn (no integer `Div`/`Mod`, no trap-capable `Index`/`Update`, no `Print`/token use, and transitively-clean callees over `Call` + `Map`/`Fold` body edges — the `TrapCaps` fixpoint spirit). Clean fns emit `readonly nounwind`, plus `willreturn` when the closure has no `LoopEnter` and no call/body cycle (a `willreturn` violation is UB — a possibly-diverging loop must never carry it); a clean fn's bare-`ptr` by-ref parameter additionally gets `noalias nocapture readonly` (never written through, never escapes, the single pointer argument). Unclean fns stay bare, so trap behavior and print ordering are untouched by construction. `flow_trap` is declared `noreturn` (flow-rt defines it `-> !`); print externs stay attribute-free. **Wave-4 refinement:** a `bounds_proof`-PROVEN `Index` is not trap-capable (its guard is elided — no trap path exists), so it no longer counts against the clean set (`func.rs:FnAttrs::analyze` computes the per-fn proof plan); `Update` stays counted (conservative this wave).
- **Proven-`Index` guard elision (S20 `bounds_proof` consumer, applied 2026-07-22, wave 4)** — `func.rs:FnEmit::bp` holds the fn's `bounds_proof` plan (computed once in `FnEmit::new`, next to `lup`); `emit_index(m, …)` drops the two-sided guard (`icmp slt`/`icmp sge`/`or` + the `flow_trap(1)` block) when `bp.proven(m)` — the plan shows the index statically inside `[0, n)`, so the trap is dead and trap-freedom is exact — emitting just the type-directed extension + GEP + load. Everything unproven (unknown/wrapping/loop-carried indices — fir's `coeffs[k]`, the one-kernel matmul's fold body, suggestion #9) emits today's guard byte-identical; `Update` keeps its guard regardless. The trap kind+1 encoding, exit-101 protocol, and `guard_index` itself are unchanged — only dead guards disappear.
- **`Iota`/`Fill` (ADR-0029, stage 1, applied 2026-07-22)** — the array-construction primitives emit as `i64`-indexed store loops (`func.rs:emit_iota`: `out[i] = trunc i64 iv to i32`, count read from the target type — validate ties it to the `Constant` source; `func.rs:emit_fill`: `out[i] = x` from the internal pair's slot 0). Trap-free by construction (no guard, no trap param). Compile-and-run oracle parity pinned by `tests/differential.rs::differential_iota_fill`.
- **`zeroext` ABI** — every `i8`/`i1` param carries `zeroext` in the `declare` lines and at every call site; the attr goes **after** the type in call args (`i8 zeroext %v`) — the S13 fix for the invalid-LLVM attr-before-type form that clang's own bug-hunt caught.
- **Determinism (L2)** — names from per-fn ordinals + a rising counter, never slotmap bits; emit twice is byte-equal.
- **Differential harness** — emit → `clang <prog>.ll libflow_rt.a -o prog` → run (time-boxed) → compare per L1, **at both `-O0` and `-O2`** for every case (the DESIGN §8 `-O2` row, added in the post-review pass; optimization is where accidentally-relied-on LLVM-level UB would surface); examples raw + rewritten, closed-mode testgen sweep (≥256 cases × raw+rewritten, fanned across `available_parallelism` threads), u8 ABI, matmul loop-driven Update, traps exit-101. Render parity to interp is pinned in flow-rt's unit table.

## What does not / known issues

- **Nested loops (multi-merge SCC) ⇒ `EmitError::Unsupported { "nested loops" }`** (L3/BL6; `lib.rs:emit` gate) — the same scope boundary as interp M1 and rewrite RW8; lifted across the toolchain in one increment or not at all.
- **Alloca-slot stack ceiling (BL1, the remaining face)** — whole array aggregates live in the frame. The sepia perf shape at N = 262144 holds ~9 MB of `[Pixel; N]` allocas, over the 8 MB default; the perf test raises it via `ulimit -s hard` (`perf_baseline.rs:run_big_stack`). Examples/testgen arrays are tiny and never approach it. Heap lowering remains the headroom that lifts this ceiling. **BL1's other two faces died in S21:** the literal-store module blowup (ADR-0029 procedural sources — 23 MB `.ll` → 21 KB) and the first-class-aggregate optimizer blowup (WP3b — staging copies were `load [65536 x double]` SSA values; clang -O2 on matmul256_cap went from OOM-kill/222 s+ to 0.08 s / 57 MB; `capture_array_staging_never_loads_whole` pins the shape).
- **Perf N capped at 4096** — the array literal is N `Pair` stores (Core has no array-fill); at N = 262144 clang `-O2` needs >25 min CPU on the ~1M-line module and 65536 is still minutes (observed S13). 4096 already shows the scale story unambiguously; an array-fill primitive / heap lowering restores large N.
- **Diverged testgen programs are skipped** in the differential (no finite native observable); open-mode `i32 → i32` testgen is excluded (no native `@main` analog, BL8).

## Invariants enforced (and where in code)

L1 oracle parity (`Done` ⟺ exit 0 + stdout byte-equal; `Trapped` ⟺ exit 101 — `differential.rs:expect_native`/`assert_parity`); L2 determinism (`golden_ll.rs:determinism_emit_twice_byte_equal`); L3 capability gate (`lib.rs:emit` `loop_plan(...).is_some()` per merge, `golden_ll.rs:nested_loop_is_unsupported`); L4 token erasure (`ty.rs:lower_ty` returns `None`, no slot allocated in `func.rs:emit`); guard-first decide/advance split (`loops.rs:emit_loop` on `loop_plan`); strict `select`-Phi (`func.rs:emit_morphism` `Phi` arm, BL2); type-directed index guard (`func.rs:guard_index`/`load_index`) with the proof-driven elision keyed on `bounds_proof` (`func.rs:emit_index` on `FnEmit::bp` — only plan-proven guards drop).

## Test coverage (golden / property / differential / skipped+why)

- **golden (`tests/golden_ll.rs`, 16 tests — S21 added `widen_emits_conversion_instructions` (exact `sext`/`sitofp`×2/`fpext` counts on the four-edge lattice) and `capture_array_staging_never_loads_whole` (WP3b regression: a capture-form program's `.ll` contains ZERO whole-array `load [`); WP3b re-pinned 3 snapshots — capture staging became pointer fields, Update/route copies became `llvm.memcpy`, each diff hand-read):** `golden_examples` (10 insta snapshots) + `golden_micro_{arith,update,two_loops}` (3 snapshots) + `golden_update_inplace_carried_loop` (suggestion #2: the matmul4-class loop-carried `c[t] <- v` form emits NO `llvm.memcpy` call — in-place into the merge storage; 1 snapshot) + `golden_update_memcpy_kept_when_not_dead` (the negative pins: an escape via a `Pair` field of the output product, and a by-ref ptr-resident source, both keep the copy) + `golden_capture_{map,fold,one_kernel_matmul}` (3 ADR-0027 snapshots — the matmul one pins the by-ref array-capture shape, suggestion #6) + `golden_proven_index_guard_elision` (S20 wave 4: the matmul-cell-class map body emits ZERO `icmp slt`/`sge` index-guard instructions for the proven affine index — the signed `Div`/`Mod` `icmp eq` guards stay — while the unproven sibling, an index by an unknown parameter, keeps exactly one guard + one `flow_trap(1)`; 1 snapshot) + `golden_proven_index_fn_clean_attrs` (the `FnAttrs` proof refinement: the enumerate-`.0` cell body carries `readonly nounwind willreturn` with zero `icmp`; a bare-Array Named fn over constant indices carries `ptr noalias nocapture readonly` + `readonly nounwind willreturn`; 1 snapshot) + `determinism_emit_twice_byte_equal` + `nested_loop_is_unsupported` (L3 pin) + `exit_only_payload_emitted_once` (driver-ownership pin) = 19 golden `.ll` snapshots.
- **differential (`tests/differential.rs`, 9 tests):** `differential_examples_raw_and_rewritten` (the M2 line — 10 examples × raw+rewritten), `differential_two_sequential_loops` (→ `"20\n"`), `differential_traps_exit_101` (div0 + OOB Update), `differential_u8_index_and_print` (the u8 `zext`+guard + `i8 zeroext` ABI — the sole compile-and-run cover for that class), `differential_testgen_closed_sweep` (≥256 closed cases × raw+rewritten), `differential_matmul_loop_driven_update` (loop-carried `mut c` + `c[t] <- v` with the per-element `cell(a, b, i, j)` call — the by-ref-call-args shape, → `"8\n136\n"`), `differential_captures` (map/fold captures + the one-kernel matmul, → `"-275\n3748\n"`), `differential_computed_exit_payloads` (exit-only payload + exit-arm captured map), `differential_byref_call_args` (suggestion #8's escaping-use shapes: whole-input-product return, `zip` straight on the by-ref input product). Every case is compiled and run at **both `-O0` and `-O2`** against the same oracle expectations (the DESIGN §8 `-O2` row, added 2026-07-18). All skip-with-reason when `clang` is absent (never a faked pass).
- **`-O2` differential row — first full run (2026-07-18, post-review pass):** `cargo test -p flow-backend-llvm -- --nocapture` on an Apple M4 Pro (14-core arm64, Darwin 25.3.0), Apple clang 17.0.0 (clang-1700.6.3.2). All 6 differential tests green at both opt levels; the sweep covered 320 closed testgen programs (256 default + 64 trap-free) × raw+rewritten = 640 jobs × 2 opt levels = 1280 compile-and-runs, **zero divergences** — no LLVM-level UB surfaced. Sweep wall time ~347 s compile+run under a heavily loaded shared machine (7-way parallel pass; ~50 ms per clang invocation idle), generation+emission ~0.7 s.
- **render parity (`crates/flow-rt`, 1 test):** `render_parity` table — f64/f32 (`4080.0`, `5.375`, `-0.0`, `NaN`, `inf`, MAX), u8 (0/127/128/255), bools, Str, i32/i64 extremes, each byte-equal to `flow_interp::render`.
- **perf (`tests/perf_baseline.rs`, 1 test, `#[ignore]`):** `sepia_perf_baseline`.

## Performance notes (numbers + bench name + date; regressions flagged)

**Proven-`Index` guard elision (S20 wave 4; 2026-07-22, Apple M4 Pro arm64, Homebrew clang 22.1.8 `-O2`, min-of-3):** `benches/matmul/matmul64_cap.flow` — the `.ll` diff is exactly 54 lines: `flow_main`'s two constant result-index guards (`c[0]`/`c[4095]` — 4 `icmp` + 2 `flow_trap` blocks) gone, module totals 17 → 13 `icmp` / 6 → 4 trap calls. **11.72 ms → 11.34 ms** native `-O2` — process-spawn floor-bound, within noise in kind, because the hot fold body is unchanged this wave: the one-kernel matmul's inner fold rides a *captured* literal ramp whose element range the shipped `bounds_proof` cannot see through the by-ref capture Proj (and the captured `i`/`j` are unquantified), so its two per-iteration guards stay (the follow-up is suggestion #9, ir-side; the consumer is already plan-driven, so new proofs drop guards with no emitter change). What DID land: every constant/enumerate-`.0`/affine-cell-class `Index` in the module set is guard-free (vector_add/zip_demo `c[0]`/`c[15]`, capture_map `b[1]`, the update-elision golden's `r[2]`), and proof-clean bodies now carry `readonly nounwind willreturn` (+ `noalias nocapture readonly` on a bare-`ptr` param) for LLVM's cross-call reasoning. Oracle-equal everywhere: `-275`/`3748` (`matmul4_cap.flow`, 1.91 → 1.80 ms), `1047`/`2107` (`matmul64_cap.flow`), each emit+compile+run at `-O2`; the full differential (320 closed testgen cases × raw+rewritten × `-O0`/`-O2`, exercising `Index` constantly) stays zero-divergence. The znver2 re-measure (the 36× loop / 115× capture sweep gap) is a later box wave — its prime suspect, the fold-body guards, awaits suggestion #9.

**Last-use Update elision (suggestion #2; 2026-07-22, Apple M4 Pro arm64, Homebrew clang 22.1.8, min-of-3 shell loop):** `benches/matmul/matmul64.flow` (N=64, loop-carried `mut c` + `c[t] <- v`) — the per-iteration whole-array `llvm.memcpy` (16 KB × 4,096 iterations) is gone from the `.ll` (the update target shares the merge view's slot; structural pin `golden_update_inplace_carried_loop`). Native `-O2` stays **0.01 s → 0.01 s** — already at the process-spawn floor after #8, as plan-last-use §5 predicted ("already at the 0.01 s floor locally — the sweep keeps it there"); `-O0` is unchanged in kind (~1.6 s → ~1.7 s — the elision removes one of the loop's per-iteration aggregate copies; the remaining merge/back-route copies are the loop driver's dance, not this row's). Oracle-equal everywhere: `-275`/`3748` (`examples/matmul4.flow`), `1815`/`6944` (`matmul16.flow`), `1047`/`2107` (`matmul64.flow`), each emit+compile+run at both `-O0` and `-O2`; the full differential (320 closed testgen cases × raw+rewritten × `-O0`/`-O2`, incl. `differential_matmul_loop_driven_update`) stays zero-divergence.

**Loop-form matmul, by-ref array call args (suggestion #8; 2026-07-21, Apple M4 Pro arm64, Homebrew clang 22.1.8 `-O2`, min-of-3 shell loop):** `benches/matmul/matmul64.flow` (N=64, loop-carried `mut c` + `c[t] <- v`, per-element `cell(a, b, i, j)` calls) — **0.33 s → 0.01 s** (~33×; 0.01 s is the `/usr/bin/time` resolution floor, process-spawn-dominated — the same floor the capture form reached in wave 1). Before the change each `cell` call copied both `[4096 x float]` arguments inline (~32 KB × 4,096 calls ≈ 134 MB of memcpy, the bulk of the S19 loop-form 316 ms); after, both arrays pass as `ptr` and no array bytes cross a call boundary. Oracle-equal: `1047`/`2107` at N=64, `1815`/`6944` at N=16 (`matmul16.flow`, both `-O0` and `-O2`). **Regression check:** the capture-form `matmul64_cap.flow` still runs in the 0.01 s class (min-of-3) after both wave-2 rows — no regression from the attribute/by-ref work.

**Capture-form one-kernel matmul, by-ref array captures (suggestion #6; 2026-07-21, Apple M4 Pro arm64, clang `-O2`, min-of-3 shell loop):** `benches/matmul/matmul64_cap.flow` (N=64, one map over cells × inner fold over captured `a`/`b`) — **2.08 s → 0.01 s** (~200×; 0.01 s is the `/usr/bin/time` resolution floor, process-spawn-dominated). Before the change the fold body took `{ [4096 x double], i32, [4096 x double], i32, double, i32 }` by value ≈ 17 GB of inline-array copies across 262,144 calls (the S19 measurement); after, both body signatures are `ptr`-shaped and no array bytes cross a call boundary. Output byte-equal to the interp oracle (`1047`/`2107` at N=64; `-275`/`3748` at N=4, `matmul4_cap.flow`, both `-O0` and `-O2`).

`sepia_perf_baseline` (2026-07-18, S13, idle machine, arm64 -O0/-O2 vs interp; `cargo test -p flow-backend-llvm --test perf_baseline -- --ignored --nocapture`):

| N | interp | native -O0 | native -O2 |
|---|---|---|---|
| 16 | 1.53 ms | 4.60 ms | 4.61 ms |
| 4096 | 387.36 ms | 4.84 ms | 4.82 ms |

Native time is process-spawn-dominated (flat ~4.8 ms across N); at N = 4096 the compute gap is ~**80×** in native's favor and grows with N. First baseline; no regressions tracked yet. Larger N blocked on the array-literal module size (bullet above).

## Open questions (→ ADR candidates)

- ~~`-O2` differential row~~ — **done 2026-07-18**: every differential case runs at `-O0` and `-O2`; first full run green, zero divergences (record in Test coverage above).
- `frem` vs Rust `%` parity for float `Mod` (DESIGN §8) — pin with a differential case; if `frem` diverges, call `fmod` from flow-rt instead.
- Nested-loop emission increment (with interp + rewrite, BL6).
