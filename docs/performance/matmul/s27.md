# Matmul — S27 (2026-07-24): FMA product face + rung 3 packing + panel residence + loop→map lifting — LOCAL

Square GEMM `c[i][j] = Σₖ a[i,k]·b[k,j]`. Times in **ms**, min of runs shown.
**One machine, LOCAL: Apple M4 Pro** (arm64 NEON, 14 threads, 48 GB, Homebrew
clang 22.1.8; spec row stamped atop the CSV). Box leg: see `s28.md` (ran in S28).
Raw: `benches/matmul/results-s27-local.csv` (one-machine rule).
Basis: **compute only** — flow = `FLOW_PERF` llvm kernel timer, cpp/rust/numpy
self-time per-iteration; **no chapel locally** (chpl not installed — box-only leg).

**What changed since S26:** (1) **FMA contraction, product face** — the tiled
kernel emits per-instruction `contract` flags (`EmitOpts::contract`; the driver
flag never retrofits textual IR since LLVM 14 — golden-verified); `-fma` legs are
the product face, **numerically-equal-not-byte-equal** to the conformance legs by
design (single rounding; rel-tol + disasm gated). Conformance legs stay bit-exact
to the interp oracle. (2) **Rung 3 packing + panel residence** — b is read through
a packed j-tile-major 64-aligned panel; the nest is j-tile-outer so the panel
stays resident in private L2 across each thread's row blocks (b-traffic
÷i_blocks/thread). On this machine the residence gain is ~nil (M-series SLC
already held b) — the zen2/3 box is its test (s28.md). (3) **Per-width TJ**
(f64→8), k-unroll ×2, prefetch. (4) **loop→map/fold lifting (S27b)** — the
`flow-llvm` LOOP-form legs below now lift to map{fold} and tile: same emission
class as the cap legs.

## Caveats before the tables (honesty rows)

- **numpy here is Apple Accelerate (AMX/SME matrix coprocessor)**, not OpenBLAS
  on NEON: 3.2 TFLOP/s @1024 f32 — different silicon than the NEON pipes every
  other leg uses. It is the platform BLAS and belongs in the table, but the gap
  is coprocessor-vs-SIMD, not kernel-vs-kernel. `OPENBLAS_NUM_THREADS=1` does
  NOT pin Accelerate — the numpy-1t rows are identical to numpy and are dropped
  as unmeasurable locally. numpy runs **fp32** (the standing S26b pairing flag).
- **Flow 2048/4096 rows are absent locally**: 3 arrays + packed panel exceed
  macOS's 64 MB hard stack ceiling (`ulimit -s hard`). Baselines (heap arrays)
  carry 2048. llvm heap lowering is the recorded enabler (S29, in flight).
- cpp/rust mt = row-partitioned `std::thread`/scoped threads, T=14
  (`hardware_concurrency`; no cgroup locally).
- Every leg's two printed outputs agree byte-for-byte at every shared N across
  all languages (`out=` columns in the CSV) — incl. the lifted loop-form legs.

## CPU compute (ms, min-of-3 — flow kernels only; both faces)

| N | f32 conformance | f32 fma | f64 conformance | f64 fma |
|---|---|---|---|---|
| 256 | 0.59 | 0.43 | 0.78 | 0.78 |
| 512 | 2.14 | 1.86 | 3.19 | 3.08 |
| 1024 | 7.55 | **6.75** | 12.67 | **10.84** |

fma compute gain @1024: f32 1.12×, f64 1.17× at 14 threads (memory-bound);
single-thread it is larger — see the 1t rows below.

## Who wins — 1t on 1t (both sides single-thread, compute)

| N | flow f32 fma | cpp f32 | rust | flow ÷ ahead | flow f64 fma | cpp f64 | flow ÷ ahead |
|---|---|---|---|---|---|---|---|
| 512 | 2.52 | 87.99 | 81.17 | **35× / 32×** | 5.00 | 103.21 | **20.6×** |
| 1024 | 19.71 | 842.55 | 851.75 | **42.7× / 43.2×** | 39.22 | 871.39 | **22.2×** |

(Conformance-face 1t compute @1024: f32 35.36 → fma 19.71 = **1.79×**; f64
67.40 → 39.22 = **1.72×** — the FLOP-density win where compute-bound.)

## Who wins — par on par (both sides at their best threading, compute)

| N | flow f32 fma | cpp-mt f32 | rust-mt | flow ÷ ahead | flow f64 fma | cpp-mt f64 | flow ÷ ahead |
|---|---|---|---|---|---|---|---|
| 256 | 0.43 | 1.19 | 1.18 | **2.8× / 2.7×** | 0.78 | 0.93 | 1.2× |
| 512 | 1.86 | 9.94 | 7.24 | **5.3× / 3.9×** | 3.08 | 13.27 | **4.3×** |
| 1024 | 6.75 | 132.41 | 131.31 | **19.6× / 19.5×** | 10.84 | 128.11 | **11.8×** |

Flow is ahead at every size, small N included. Accelerate stays ahead of
everything NEON-class (0.66 ms @1024): the AMX coprocessor row is the platform
ceiling, not a same-silicon comparison; the kernel target to close is OpenBLAS
(box rows in s28.md).

## The lift, measured (S27b)

`flow-llvm` loop-form legs (`examples`-style `mut`/`Update`/cross-fn-`Call`
matmul — `matmul4..128.flow`) now emit the same packed tiled kernels as the cap
forms (byte-level tile markers verified; outputs byte-equal to every other leg).
The structural fact is the point: **the naive loop program and the map/fold
program are now the same emission** (S26's non-tiling pin inverted).

## vs S26 local (same machine class — M-series, 1t compute @1024 f32)

S26: tile 35.0 → S27: conformance 32.8 (pack+residence+unroll) → **fma 18.9 ms
(1.85× vs S26)**. f64: ~66.7 → 64.0 → **38.6 (1.73×)** (TJ=8 + fma).

## Shapes — fir / conv2d cross-language (LOCAL, `benches/shapes/shapes_ab.sh`)

New this session: cpp (naive + `std::thread` mt), rust (naive + scoped-thread
mt), and numpy baselines computing byte-identical outputs to the flow shapes
(verified per run; fma legs rel-tol ≤1e-4). attn waits on `exp` (Core has none
— standing open question). Min-of-3 ms, same machine; variance between
invocations is real (P/E cores) — read ratios coarsely. **S28 rungs landed on
these tables — see s28.md.**

| shape | flow conf par | flow fma par | flow fma 1t | cpp 1t | cpp mt | rust 1t | rust mt | numpy |
|---|---|---|---|---|---|---|---|---|
| fir 65536×64 | 0.47 | **0.36** | 1.14 | 0.61 | 0.21 | 0.92 | 0.24 | 0.39 (correlate, 1t C) |
| conv2d 512²×3×3 | 0.51 | 0.57 | 1.20 | **0.065** | 0.13 | 0.44 | 0.21 | 0.43 |

Readings:
- **fir**: flow lands mid-pack — behind cpp-mt/rust-mt ~1.5–1.7×, near numpy's
  single-thread C correlate. 1-D sites ride rung 1 only (no TI blocking, no
  packing — recorded S27 ceiling), and 64-tap FIR is the S25 "small-K clang
  self-SLPs" class: the baselines' fixed-64 inner loop fully vectorizes on its
  own. FIR packing/blocking is un-cashed headroom.
- **conv2d — the priced refusal**: flow is **7.9× behind SINGLE-thread cpp at 14
  threads** (0.51 vs 0.065; 18× at 1t). The site is the recorded `tile_plan`
  refusal (non-affine `k/3`,`k%3` in the fold body) → flow emits the naive
  per-cell div/mod fold while clang unrolls the compile-time 3×3 into pure SIMD.
  The "conv2d derived-var walker + conv→matmul rewrite" agenda item was gated on
  measured demand — **this is the measurement**. (cpp-mt slower than cpp-1t:
  64 µs of work, spawn cost dominates — honest row, not an error.)

Standing next (S28, all landed or in flight): box run (s28.md — 2048/4096 rows +
chapel legs + true numpy-OpenBLAS pairing); conv2d derived-var walker (landed);
FIR 1-D blocking rung (landed); attn legs once `exp` lands.
