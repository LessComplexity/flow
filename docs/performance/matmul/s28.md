# Matmul + shapes — S28 (2026-07-24): k-split record, FIR window rung, conv2d micro-kernel; box matrix (EPYC 7B13 zen3)

Two legs: **local** (Apple M4 Pro, arm64 NEON, 14 threads) and **box** (AMD EPYC
7B13 64-Core zen3, 512K L2/core, 61.44-core cgroup quota — flow T=62, cpp-mt
T=62, rust-mt T=61, clang 18.1.8, ubuntu 22.04, vast.ai on-demand, destroyed
after ≈$0.45). Box CSV: `benches/matmul/results-s27.csv` (spec-stamped, one box).
Basis: compute only — flow `FLOW_PERF` kernel timer vs baselines' self-timed
iteration. numpy: local = Apple Accelerate (AMX coprocessor, fp32), box =
OpenBLAS. Every leg's outputs byte-verified per run (fma faces rel ≤ 1e-4).

**What changed (code, S28):** `tile_plan` records the fold's derived axes
(`TileRead.ksplit = {div, cq, cr}` — the map-body `t/C,t%C` move one level down;
conv2d's `k/3,k%3` site now records instead of refusing). Two new emission
rungs: **FIR 1-D window rung** (`emit_tiled_map_blocked_1d` — TI×TJ blocks over
the lane axis, one scalar `w[k]` per k shared, constant-TJ main) and **conv2d
micro-kernel** (`emit_tiled_map_conv` — 9 taps fully unrolled at compile-time
offsets, zero div/mod). Gate: full workspace 69/69 green; matmul .ll artifacts
byte-identical. Box disasm gates: conformance face 0 vfmadd; fma face 128
vfmadd / 0 unfused vmulps (the S26 "vfmadd absent" finding closed on AVX2).

## Shapes — local (M4 Pro, min-of-3, `benches/shapes/shapes_ab.sh`)

| shape | flow conf par | flow fma par | flow fma 1t | cpp 1t | cpp mt | rust 1t | rust mt | numpy |
|---|---|---|---|---|---|---|---|---|
| fir 65536×64 | 0.26 | **0.213** | **0.216** | 0.92 | 0.24 | 0.85 | 0.30 | 0.39 |
| conv2d 512²×3×3 | 0.53 | 0.51 | 0.43 | **0.070** | 0.13 | 0.45 | 0.19 | 0.43 |

- **fir: both tables WON.** fma-par 0.213 < cpp-mt 0.239 / rust-mt 0.302;
  fma-1t 0.216 < cpp-1t 0.924 (4.3×) — flow single-threaded also beats both mt
  baselines. vs S27 rung-1 (0.36 par / 1.14 1t): 1.65× / 5.3×.
- **conv2d: kernel 3× over cpp-mt; the par leg is bracket-bound.** The flow leg
  times the whole program including the untiled 264k-cell img-gen map
  (baselines time kernel-only). Decomposition: gen ≈ 0.47 par / 0.38 1t;
  kernel ≈ **0.04 par = 3× over cpp-mt 0.133** (16× over flow's S27 untiled
  fallback), ≈ 0.05 1t (parity with cpp-1t). The leg-level fix is the `time`
  builtin (kernel-scoped timing in source) — S29, in flight.

## Box — matmul matrix, compute (ms)

| N | flow conf f32 | flow fma f32 | flow conf f64 | flow fma f64 | cpp-mt f32 | cpp-mt f64 | rust-mt | chapel f32 | chapel f64 | numpy (OpenBLAS) |
|---|---|---|---|---|---|---|---|---|---|---|
| 512 | 4.22 | 5.57 | 5.22 | 5.43 | 6.82 | 12.56 | 6.91 | 5.99 | 8.77 | **0.39** |
| 1024 | 13.10 | 18.15 | 18.59 | 18.67 | 122.63 | 102.73 | 79.16 | 96.99 | 103.18 | **1.87** |
| 2048 | 46.38 | 57.89 | 87.54 | 84.71 | 1097.5 | 1135.0 | 857.0 | 1129.1 | 1170.4 | **8.13** |
| 4096 | 275.5 | 283.5 | 416.6 | 470.9 | 9305.1 | 9727.0 | 7737.3 | 9575.5 | 9900.1 | **48.12** |

flow-fma ahead of every language baseline at every size (1024 f32: 6.8× cpp-mt,
4.4× rust-mt, 5.3× chapel; naive-layout baselines fall off a cache cliff past
1024). **OpenBLAS threaded is the frontier: 9.7× @1024, 5.9× @4096** (2.86
TF/s @4096) — the S29 levers (KC k-panel, A-packing) are in flight.

## Box — 1t (flow rows are process-wall incl. floor; floor ≈ 5 ms, material
only at 1024 — compute-1t legs land with the next box run)

| N | flow fma-1t f32 | flow conf-1t f32 | flow fma-1t f64 | cpp-naive f32 | rust-naive | chapel-1t f32 | numpy-1t (OpenBLAS) |
|---|---|---|---|---|---|---|---|
| 1024 | 55.27 | 70.62 | 107.04 | 4982.0 | 3783.4 | 4439.4 | **20.54** |
| 4096 | 2484.9 | 3682.0 | 5149.7 | 498245 | 412890 | 581266 | **1311.3** |

flow 70–90× over the naive language baselines single-threaded. **numpy-1t
OpenBLAS 2.7× ahead @1024** — KC k-panel / A-packing target this gap.

## Box — shapes (S27-baseline code → S28 rungs, same protocol)

| shape · leg | S27 flow | S28 flow | cpp 1t | cpp mt | rust mt | numpy-1t |
|---|---|---|---|---|---|---|
| fir par | 0.316 | 0.526 (61T) / **0.287 (16T)** | 2.76 | 2.43 | 1.93 | 1.39 |
| fir 1t | 0.925 | **0.786** | 2.78 | (2.74) | (1.98) | 1.35 |
| conv2d par | 0.752 | **0.742** | 0.116 | 2.32 | 1.64 | 1.65 |
| conv2d 1t | 2.171 | 2.250 | **0.123** | (2.41) | (1.64) | 2.48 |

- **fir: box tables WON at both thread counts** (best baseline leg 1.39).
  The 61T-vs-16T spread is slice quantization, measured: 16 slices over a
  61-thread pool = 0.26 waves vs 1.0 wave at 16T. Fix: slice-count-aware grain
  (flow-rt; suggestion #15). The 0.316→0.526 par delta is the same artifact,
  not the rung (1t improved 0.925→0.786, 16T wins outright).
- **conv2d par table WON at the leg level on the box** (0.742 vs cpp-mt 2.32 —
  EPYC spawn cost makes the 116 µs-of-work mt leg uncompetitive). 1t leg
  bracket-bound as local (untiled img-gen ≈ 2.1 of 2.25 at EPYC per-core
  speeds).

Standing next (S29, in flight this session): KC k-panel split + A-panel packing
(OpenBLAS gap), heap lowering (2048/4096 local), `time` builtin (kernel-scoped
legs), bigger shapes sizes (fir 1M+, conv2d 1024+), conv TI rows + im2col.
