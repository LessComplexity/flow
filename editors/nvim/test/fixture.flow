// Fixture for syntax_test.lua. Line numbers are asserted — do not reflow.
fn double(x: i32) -> i32 {
    x * 2 -> ret;
}

fn shout(x: i32) {
    x -> println;
}

fn main() {
    5 -> a;
    a -> shout;
    a -> double -> b;
    16 -> iota -> ti;
    3 -> widen_f32 -> f;
    b -> cmp;
    cmp
        -true-> a;
        -false-> b;
    16 -> iota -> map { t -> t } -> arr;
    Pixel { r: 1.0, g: 2.0, b: 3.0 } -> px;
}

fn labeled() {
    :myloop {
        x -> y;
        -> :myloop;
    }
}

fn extras(n: i32, arr: [i32; 4]) -> i32 {
    "tab\there\n" -> print;
    (arr, arr) -> zip -> pairs;
    arr -> enumerate -> indexed;
    arr[0] <- 7;
    n -> which;
    which
        -0-> zero;
        -42-> answer;
        -_-> fallback;
        -Some(x)-> some_case;
        -[]-> empty;
        -[head, ...tail]-> split;
    n -> ret;
}
