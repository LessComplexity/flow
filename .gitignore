/target
__pycache__/
.DS_Store

# Emitted artifacts. Regenerated from the .mapal sources by benches/matmul/regen.sh;
# the sources and the emitter are the record, not their output.
benches/matmul/*.ll
benches/matmul/*.cu

# built artifact — regenerate with package-vsix.py
editors/vscode/*.vsix
